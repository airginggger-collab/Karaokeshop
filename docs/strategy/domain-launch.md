# Запуск домена karaokeshop.kz + перенос с Wix + GSC

> Как перевести боевой домен `karaokeshop.kz` со старого Wix на новый сайт (Cloudflare Worker), **не потеряв позиции в Google**. Плюс SEO-чек-лист. Контекст: сейчас новый сайт живёт на `*.workers.dev`, а `karaokeshop.kz` отдаёт старый Wix (см. [/CLAUDE.md](../../CLAUDE.md) ловушка 7).

## Решение по домену
**Остаёмся на `karaokeshop.kz`** (без дефиса — сильнее ворованного `karaoke-shop.kz`, есть история в Google). Новый домен = обнулить SEO-авторитет. Переезд — в пределах того же домена, значит «смена адреса» в GSC НЕ нужна, ранжирование сохраняется при корректных 301.

## Канон: www
`siteConfig.url = https://www.karaokeshop.kz` → канон = **www**. Все `canonical`/`og`/`sitemap` уже на www.
⚠️ **Без-www → www нельзя сделать в `_redirects`** (Cloudflare Workers Static Assets разрешает только относительные пути; абсолютный URL → деплой падает: «Only relative URLs are allowed»). Хост-канонизацию настроить **Redirect Rule** в дашборде Cloudflare (домен → Rules → Redirect Rules): source `www.karaokeshop.kz/*` → нет; наоборот — `karaokeshop.kz/*` → `https://www.karaokeshop.kz/${1}` 301. `_redirects` — только для относительных старый→новый путей.

---

## Чек-лист переезда (по шагам)

### До переключения DNS
- [x] **Инвентаризация старого Wix** — сделано 2026-07-21: снят `sitemap.xml` старого сайта (индекс + 6 суб-сайтмапов), 77 URL. GSC-выгрузка по запросам не делалась (sitemap покрывает проиндексированное).
- [x] **Карта редиректов старый→новый** — сделано 2026-07-21: `apps/web/public/_redirects`, секции 2а–2г (точные пары выше сплатов; кириллические URL Wix добирает `/product-page/*`).
- [x] **Контент-паритет:** новый сайт покрывает все живые разделы старого (каталог/бренды/дом/бизнес/сравнение/блог); демо-мусор шаблона Wix (`/items`, `/news*`) паритета не требует → 301 на `/` и `/blog`.
- [x] **Проверить сборку:** `_redirects` в `out/` ✓, `check-redirects` ✓, 50 тестов web ✓ (2026-07-21).

### Переключение (cutover)
- [ ] **Привязать домен к Worker'у** (один из вариантов):
  - **Проще — дашборд:** Cloudflare → Workers & Pages → `karaokeshop` → Settings → Domains & Routes → **Add Custom Domain** → `karaokeshop.kz` и `www.karaokeshop.kz`. Cloudflare сам заведёт DNS + SSL.
  - **Или wrangler** (добавить НА МОМЕНТ cutover, не раньше — иначе `wrangler deploy` упадёт, пока зоны нет в аккаунте):
    ```toml
    routes = [
      { pattern = "karaokeshop.kz", custom_domain = true },
      { pattern = "www.karaokeshop.kz", custom_domain = true },
    ]
    ```
- [ ] **DNS:** если домен не на Cloudflare — сменить NS у регистратора на Cloudflare (или добавить зону и A/CNAME на Worker). Дождаться распространения.
- [ ] **Не отключать Wix**, пока новый сайт не отвечает на домене и редиректы не проверены (страховка отката).

### После переключения
- [ ] **Проверить:** `https://karaokeshop.kz` и `www` отдают новый сайт; без-www → www (301); несколько старых URL → новые (301); `https://www.karaokeshop.kz/sitemap.xml` и `robots.txt` живые; `og.jpg` отдаётся (200).
- [ ] **Отписаться от Wix** после подтверждения.

---

## Google Search Console
- [ ] Ресурс типа **«Домен»** `karaokeshop.kz` (покрывает http/https, www/без-www). Верификация — **DNS TXT-записью** (удобно добавить в Cloudflare DNS).
- [ ] Отправить `sitemap.xml`.
- [ ] «Проверка URL» → запросить индексацию ключевых страниц (главная, каталог, топ-товары, лендинги).
- [ ] Следить: Индексирование → Страницы (ошибки/404), Покрытие, Sitemap. Чинить редиректы.
- [ ] «Смена адреса» — **НЕ нужна** (домен тот же).
- [ ] Проверить расширенные результаты: Product/Offer/AggregateRating, LocalBusiness, FAQ, BreadcrumbList (валидатор schema.org / Rich Results Test).

## Аналитика
- [ ] **Яндекс.Метрика** — вставить реальный числовой ID вместо заглушки `XXXXXXXX` в [`apps/web/src/app/layout.tsx`](../../apps/web/src/app/layout.tsx) (`YM_ID`). Гард `YM_ENABLED` включит счётчик сам.
- [ ] **Google Search Console** — обязательно (см. выше). GA4 — опционально.
- В РК поиск: Google доминирует (~85–90%), Яндекс ~10–15% → приоритет Google, но зарегистрировать и Яндекс.Вебмастер.

## Локальное SEO (сильный рычаг для «Алматы»)
- [ ] **Google Business Profile** (шоурум, ул. Муканова 8) + фото + категории + отзывы.
- [ ] **2ГИС** — карточка организации (в РК критично).
- [ ] **Яндекс.Бизнес**.

## Известные долги (закрыть до/после запуска)
- [ ] Реальные фото товаров (сейчас сток/демо — бьёт по доверию сильнее всего).
- [ ] Core Web Vitals: `next/image`, LCP (фактор Google) — отдельная задача.
- [ ] Длинные `<title>` на части страниц (особенно блог из-за суффикса `| karaokeshop`) — подрезать до ~60 симв.
- [ ] Дубль `description`: `/checkout` наследует дефолтный (как у `/`) — задать свой или `noindex` (checkout — транзакционная, можно `noindex`).

---

## Текущее состояние SEO (селф-чек 71 стр.)
- **title:** 71/71 уникальны ✓. Но у ~40 длина > 60 симв (обрежутся в SERP; худшие — блог 90–112). Не критично, полировка.
- **description:** 70/71 уникальны; 1 дубль (`/` ↔ `/checkout`). Пара коротких (~66 симв).
- ЧПУ-адреса: чистые, транслит, семантичные ✓. `robots`/`sitemap`(70)/JSON-LD/крошки/`og:image` — на месте ✓. `noindex` нет ✓.
- **Единственный блокер ранжирования — домен не привязан** (canonical ведёт на www.karaokeshop.kz = старый Wix). Закрывается этим переездом.
