# План Фазы 0–1 — karaokeshop.kz

> **Для исполнителя:** реализуй по задачам, отмечай шаги чекбоксами. Фаза 0 — research/setup (критерий приёмки вместо тестов). Фаза 1 — код (TDD где применимо: компоненты, SEO-утилиты).

**Goal:** Заложить SEO-фундамент (семантика, аналитика, данные, URL-карта) и поднять техкаркас (монорепо, дизайн-токены, UI-библиотека, роутинг, контент-модель, SEO-каркас) под полный ребилд.

**Architecture:** Монорепо Turborepo. Фронт — Next.js (App Router, SSR/SSG). Контент — Headless CMS (Sanity/Strapi, решается в Task 5). SEO — позвоночник: URL и IA проектируются от семантического ядра.

**Tech Stack:** Next.js 15, TypeScript strict, Turborepo, Tailwind, Style Dictionary, Storybook, Vitest, Lighthouse CI, Яндекс.Метрика + GA4.

**Опорные документы:** [стратегия](../strategy/2026-06-18-redesign-strategy.md) · [семантическое ядро](../strategy/semantic-core.md)

---

## ФАЗА 0 — Фундамент (research/setup)

### Task 1: Семантическое ядро с частотами

**Результат:** `docs/strategy/semantic-core.md` дополнен колонкой частот (W) и сезонностью по каждому кластеру.

- [ ] **Шаг 1:** Открыть [Яндекс.Wordstat](https://wordstat.yandex.ru) с регионом «Казахстан → Алматы». Снять частоты по всем запросам из кластеров A–J.
- [ ] **Шаг 2:** В Google Keyword Planner (гео Kazakhstan, язык RU) снять Avg. monthly searches по тем же запросам.
- [ ] **Шаг 3:** Отметить сезонность (помесячный тренд в Wordstat «История запросов») — гипотеза: пики перед НГ и сезоном открытия заведений.
- [ ] **Шаг 4 (приёмка):** Каждый кластер в `semantic-core.md` имеет W (Я/G) и пометку сезонности. Топ-20 высокочастотных выделены.
- [ ] **Шаг 5:** Commit — `docs(seo): частоты и сезонность в семантическое ядро`.

### Task 2: Конкурентный SEO-аудит

**Результат:** конкурентный SEO-аудит — пробелы конкурентов = наши возможности. *(Итоговый файл лежит в [`docs/research/competitor-seo.md`](../research/competitor-seo.md).)*

- [ ] **Шаг 1:** Для `1music.kz`, `art-system.kz`, `karaoke-shop.kz` выгрузить видимые URL (Screaming Frog free до 500 URL, либо `site:` оператор).
- [ ] **Шаг 2:** Зафиксировать: какие кластеры (A–J) они покрывают посадочными, а какие — нет.
- [ ] **Шаг 3:** Проверить их Core Web Vitals (PageSpeed Insights) — зафиксировать слабые места по скорости.
- [ ] **Шаг 4 (приёмка):** Таблица «кластер × конкурент» с отметкой покрытия; список незанятых кластеров (ожидаемо: сценарные, под ключ, по площади, E-E-A-T).
- [ ] **Шаг 5:** Commit — `docs(seo): конкурентный аудит и карта пробелов`.

### Task 3: Сбор продуктовых данных

**Результат:** `data/products.json` (черновой) — товары, цены, бренды, сценарий, песни, фото-ссылки.

- [ ] **Шаг 1:** Свести с текущего `karaokeshop.kz` и сайтов брендов: модель, бренд, сценарий (дом/кафе/бар/клуб), площадь, цена, состав комплекта, кол-во песен.
- [ ] **Шаг 2:** Структурировать в JSON по черновой схеме (поля станут моделью CMS в Task 11).
- [ ] **Шаг 3 (приёмка):** ≥ все актуальные системы AST и Studio Evolution занесены с ценой и сценарием; нет пустых обязательных полей.
- [ ] **Шаг 4:** Commit — `data: черновой каталог товаров`.

### Task 4: Аналитика и вебмастеры

**Результат:** заведены и подтверждены аккаунты; ID в `docs/strategy/analytics.md`.

- [ ] **Шаг 1:** Создать Яндекс.Метрику (вебвизор + цели), GA4. Записать счётчик/measurement ID.
- [ ] **Шаг 2:** Подтвердить домен в Яндекс.Вебмастере и Google Search Console (DNS-запись).
- [ ] **Шаг 3:** Завести/обновить Яндекс.Бизнес, Google Business Profile, карточку 2ГИС (единый NAP: адрес ул. Муканова офис 8, телефоны).
- [ ] **Шаг 4 (приёмка):** Все 5 сервисов активны, ID собраны в `analytics.md`, NAP идентичен везде.
- [ ] **Шаг 5:** Commit — `docs(analytics): аккаунты и ID`.

### Task 5: ADR по CMS (Sanity vs Strapi)

**Результат:** `docs/adr/0001-cms.md` — решение с обоснованием.

- [ ] **Шаг 1:** Свести критерии: резидентность данных в KZ, бюджет DevOps, скорость старта, i18n (RU/KZ), история версий, стоимость.
- [ ] **Шаг 2:** Оценить оба по критериям (таблица). Учесть: нет требования резидентности → Sanity; есть → Strapi.
- [ ] **Шаг 3 (приёмка):** ADR содержит выбор, обоснование, отвергнутую альтернативу и последствия.
- [ ] **Шаг 4:** Commit — `docs(adr): выбор CMS`.

### Task 6: Финальная URL-карта от ядра

**Результат:** `docs/strategy/url-map.md` — каждый кластер → точный URL → тип страницы → title/description-шаблон.

- [ ] **Шаг 1:** Свести кластеры (Task 1) в дерево URL (см. черновик в `semantic-core.md`).
- [ ] **Шаг 2:** Для каждого URL задать шаблон `<title>` и `<meta description>` с ключом.
- [ ] **Шаг 3:** Заложить hreflang-пары RU↔KZ (KZ-URL зарезервированы, контент позже).
- [ ] **Шаг 4 (приёмка):** Нет двух кластеров на один URL без canonical-логики; дерево покрывает A–J.
- [ ] **Шаг 5:** Commit — `docs(seo): финальная URL-карта`.

---

## ФАЗА 1 — Техкаркас + дизайн-система (код)

### Task 7: Инициализация монорепо ✅ DONE (2026-06-18)

**Files:**
- Create: `package.json`, `turbo.json`, `apps/web/`, `packages/{ui,tokens,cms-schema,config}/`

- [x] **Шаг 1:** Создать Next.js приложение:

```bash
cd /Users/platonus/Desktop/karaokeshop
npx create-turbo@latest . --package-manager pnpm
npx create-next-app@latest apps/web --ts --app --tailwind --eslint --src-dir --import-alias "@/*"
```

- [x] **Шаг 2:** Создать пустые пакеты `packages/tokens`, `packages/ui`, `packages/cms-schema`, `packages/config` с `package.json` (name `@kk/tokens` и т.д., `"private": true`).
- [x] **Шаг 3:** Проверить сборку.
- [x] **Шаг 4:** Commit — `chore: монорепо Turborepo + Next.js app`.

### Task 8: Дизайн-токены ✅ DONE (2026-06-18)

**Files:**
- Create: `packages/tokens/tokens.json`, `packages/tokens/build.mjs`, `packages/tokens/dist/tokens.css`

- [x] **Шаг 1:** Описать базовые токены (цвет, радиус, типографика) в `tokens.json` в формате Style Dictionary. Заложить light/dark.
- [x] **Шаг 2:** Установить и настроить сборку:

```bash
pnpm --filter @kk/tokens add -D style-dictionary
```

```js
// packages/tokens/build.mjs
import StyleDictionary from 'style-dictionary';
const sd = new StyleDictionary({
  source: ['tokens.json'],
  platforms: { css: { transformGroup: 'css', buildPath: 'dist/', files: [{ destination: 'tokens.css', format: 'css/variables' }] } },
});
await sd.buildAllPlatforms();
```

- [x] **Шаг 3:** Собрать токены.
- [x] **Шаг 4:** Импортировать `tokens.css` в `apps/web/src/app/globals.css`.
- [x] **Шаг 5:** Commit — `feat(tokens): дизайн-токены через Style Dictionary`.

### Task 9: UI-библиотека + Storybook (TDD) ✅ DONE (2026-06-18)

**Files:**
- Create: `packages/ui/src/Button.tsx`, `packages/ui/src/Button.test.tsx`, `packages/ui/.storybook/`

- [x] **Шаг 1: Написать падающий тест**

```tsx
// packages/ui/src/Button.test.tsx
import { render, screen } from '@testing-library/react';
import { Button } from './Button';
test('рендерит лейбл и реагирует на variant', () => {
  render(<Button variant="primary">Купить</Button>);
  expect(screen.getByRole('button', { name: 'Купить' })).toBeInTheDocument();
});
```

- [x] **Шаг 2: Запустить тест — убедиться, что падает**

Run: `pnpm --filter @kk/ui test`
Expected: FAIL — `Button` не определён.

- [x] **Шаг 3: Минимальная реализация**

```tsx
// packages/ui/src/Button.tsx
type Props = { variant?: 'primary' | 'ghost'; children: React.ReactNode } & React.ButtonHTMLAttributes<HTMLButtonElement>;
export function Button({ variant = 'primary', children, ...rest }: Props) {
  return <button data-variant={variant} {...rest}>{children}</button>;
}
```

- [x] **Шаг 4: Запустить тест — убедиться, что проходит**

Run: `pnpm --filter @kk/ui test`
Expected: PASS.

- [x] **Шаг 5:** Добавить Storybook, создать `Button.stories.tsx`.
- [x] **Шаг 6:** Commit — `feat(ui): Button + Storybook + тест`.

### Task 10: Карта сайта и роутинг-каркас ✅ DONE (2026-06-19)

**Files:**
- Create: `apps/web/src/app/(routes)/.../page.tsx` по URL-карте (Task 6)

- [x] **Шаг 1:** Создать роуты по URL-карте: 47 статических страниц (18 карточек товаров, 5 сценарных, 4 комплекта, 2 бренда, 3 блога + общие).
- [x] **Шаг 2:** Каждая страница экспортит `generateMetadata` с title/description из URL-карты.
- [x] **Шаг 3 (приёмка):** Все URL 200, сборка чиста.
- [x] **Шаг 4:** Commit. Примечание: `/sravnit` — роут есть, но не попал в `allPaths()` → пропущен в sitemap (⚠️ хвост).

### Task 11: Контент-модель в CMS ⏸ BLOCKED (ждёт решения по CMS)

**Files:**
- Create: `packages/cms-schema/src/{product,bundle,case,song,page}.ts`

- [ ] **Шаг 1:** Описать схемы по данным Task 3: `product` (бренд, сценарий, площадь, цена, история цены, комплект, песни), `bundle` (по площади), `case` (B2B-кейс), `page`/`blog`.
- [ ] **Шаг 2:** Каждой схеме добавить **SEO-поля** (title/description/og/canonical) и **локализованные RU/KZ** поля.
- [ ] **Шаг 3:** Включить версионность контента (draft/publish + history — нативно в выбранной CMS из Task 5).
- [ ] **Шаг 4 (приёмка):** Схемы валидируются CMS-CLI; запись `product` создаётся в Studio с заполненными SEO+RU полями.
- [ ] **Шаг 5:** Commit — `feat(cms): контент-модель с SEO и i18n`.

### Task 12: Ключевые экраны в Figma 🔜 TODO

**Результат:** макеты на базе утверждённых прототипов и токенов.

- [ ] **Шаг 1:** Перенести 7 прототипов (Главная, Каталог, Карточка, B2B под ключ, Готовый комплект, мобайл, checkout) в Figma с реальным визуальным брендом.
- [ ] **Шаг 2:** Связать с токенами (Tokens Studio / переменные Figma) — единый источник с `packages/tokens`.
- [ ] **Шаг 3 (приёмка):** Все экраны в desktop+mobile, компоненты вынесены, токены подключены.
- [ ] **Шаг 4:** Зафиксировать ссылку на файл в `docs/design-system.md`.

### Task 13: SEO-каркас (TDD на утилитах) ✅ DONE (2026-06-19)

**Files:**
- Create: `apps/web/src/lib/seo.ts`, `apps/web/src/lib/seo.test.ts`, `apps/web/src/app/sitemap.ts`, `apps/web/src/app/robots.ts`

- [x] **Шаг 1: Падающий тест на JSON-LD продукта**

```ts
// apps/web/src/lib/seo.test.ts
import { productJsonLd } from './seo';
test('productJsonLd содержит Offer с ценой и валютой KZT', () => {
  const ld = productJsonLd({ name: 'AST-250', price: 1500000, slug: 'ast-250' });
  expect(ld['@type']).toBe('Product');
  expect(ld.offers.priceCurrency).toBe('KZT');
  expect(ld.offers.price).toBe(1500000);
});
```

- [x] **Шаг 2: Запустить — убедиться, что падает**

Run: `pnpm --filter web test seo`
Expected: FAIL — `productJsonLd` не определён.

- [x] **Шаг 3: Реализация**

```ts
// apps/web/src/lib/seo.ts
export function productJsonLd(p: { name: string; price: number; slug: string }) {
  return {
    '@context': 'https://schema.org', '@type': 'Product', name: p.name,
    offers: { '@type': 'Offer', priceCurrency: 'KZT', price: p.price, availability: 'https://schema.org/InStock' },
  };
}
```

- [x] **Шаг 4: Запустить — PASS**
- [x] **Шаг 5:** Добавить `sitemap.ts`, `robots.ts`, hreflang-alternates в `generateMetadata`.
- [x] **Шаг 6:** Commit — `feat(seo): JSON-LD, sitemap, robots, hreflang`.

### Task 14: Lighthouse CI + бюджет CWV ✅ DONE (2026-06-19)

**Files:**
- Create: `.github/workflows/ci.yml`, `lighthouserc.json`

- [x] **Шаг 1:** Настроить `lighthouserc.json` с бюджетом: LCP < 2.5s, CLS < 0.1, performance ≥ 0.9.
- [x] **Шаг 2:** В `ci.yml` добавить шаги сборки + Lighthouse CI.
- [x] **Шаг 3 (приёмка):** PR с регрессией скорости фейлит CI.
- [x] **Шаг 4:** Commit — `ci: Lighthouse бюджет Core Web Vitals`.

---

## Self-review (покрытие спеки)
- SEO-приоритет → Tasks 1, 2, 6, 13, 14 ✓
- IA модель B → Tasks 6, 10 ✓
- Стек Next.js + CMS → Tasks 5, 7, 11 ✓
- Версионность → Tasks 8 (semver токенов), 11 (контент), 14 (CI) ✓
- Админка/самостоятельный контент → Task 11 (SEO+i18n поля в CMS) ✓
- Handoff-инфраструктура → Tasks 7 (монорепо), 9 (Storybook), 12 (Figma+токены) ✓
- Kaspi/коммерция → Фаза 3 (вне Фазы 0–1, по плану) — отмечено как граница.
- i18n RU/KZ → Tasks 6 (hreflang), 11 (локализованные поля), 13 (alternates) ✓
