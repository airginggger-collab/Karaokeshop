# HANDOFF — снимок состояния (для новой сессии)

> Краткий контекст проекта karaokeshop.kz, чтобы быстро войти в курс. Полная карта — [docs/README.md](README.md) · правила для AI — [/CLAUDE.md](../CLAUDE.md).
>
> **Обновлено: 2026-07-14 — security-ревью с фиксами: security-заголовки `public/_headers`, JSON-LD через `<JsonLd>` (20 мест), мёртвый `redirects()` при output:export заменён на `_redirects`-301 (дубль `/karaoke/dlya-doma` убран из билда и sitemap), vitest 4 + happy-dom 20 (critical-advisory закрыт) — ловушки №9–10 в CLAUDE.md. До этого 2026-07-11 — анти-AI: новое правило владельца «длинное тире «—» = AI-телл» (правило №10 в CLAUDE.md). Заход 1: убраны все видимые «—» на главной + лендингах `dlya-doma`/`dlya-biznesa` + `o-nas`/`gotovye-resheniya` (варьируя замену: запятая/точка/двоеточие/актив; диапазоны «–» и дефисы не тронуты). Осталось ~остальные страницы + блог (217) — ждёт scope. До этого stop-slop-прогон (убран пустой маркетинг-слоп в видимой прозе). До этого 2026-07-10 — правки заголовков. (1) `/dlya-biznesa`: H1 «под ключ — Алматы» → «под ключ — Алматы и по Казахстану», SEO-title → «…Алматы и Казахстан» + попутно убран пре-existing баг задвоения бренда (`| karaokeshop | karaokeshop` — title.template в layout уже добавляет бренд). (2) hero главной: форсирован перенос «Караоке-вечер / у тебя дома» (`<br/>`) и `whitespace-nowrap` на подсветке → на любом устройстве ровно две строки, убран артефакт подчёркивания под «у». До этого 2026-07-08 — отступы главной: подтянут вертикальный ритм (меньше пустоты сверху и между секциями), hero-фото выровнено вровень с заголовком (фото = высоте левой колонки через `absolute inset-0`, без пустоты под квизом). Ширина контейнера: пробовали `max-w-7xl` (1280) — владелец сказал «прибито без воздуха», возвращено к `max-w-6xl` + `sm:px-6` (`Container.tsx`). До этого 2026-07-07 — полировка облика по правкам владельца: подсветка `.hl` стала акцентным текстом с подчёркиванием (не жёлтый блок), шрифт заголовков Unbounded→Onest, нумерация секций 01-04 убрана, карточки унифицированы на `bg-background`, фото hero выровнено.** До этого в этот же день: по решению владельца возвращены анимированные счётчики (CountUp) и стоковые фото (`scenariy/*.jpg`, временно, до реальных) на главную и лендинги `/dlya-doma`, `/dlya-biznesa` — структура «Сцены» не тронута. До этого в этот же день: лого-марка перекрашена под новую палитру: тёмно-синий квадрат `#17233f` + жёлтый микрофон `#facc15` (было фиолетовое `#7c5cff` + белый). `Header.tsx` (бейдж) + `app/icon.svg` (фавикон), плюс тонкий бордер `#2a3852` вокруг квадрата (не сливается с тёмным хедером `#0e131c`). Марка фиксированная (не флипается по теме). До этого в этот же день: рецвет палитры «Сцены» — акцент сменён с фиолетового на неон-жёлтый (тёмная тема) / чернильно-синий (светлая тема), решение владельца. Значения — только в `packages/tokens/css/theme.css` (единый источник), структура/приёмы «Сцены» не тронуты. До этого: доки — создана шпаргалка дизайн-системы `docs/design/design-system.md` (токены обеих тем, типографика, радиусы, отступы + промпты Google Stitch), теперь синхронизирована с новой палитрой. До этого ЛК Фаза 2, Task 3: страница-инструкция `/admin-guide/` (noindex, «Сцена»), вход актуализирован под токен. До этого Task 2 (Sveltia CMS развёрнут на `/admin`, 11 file-коллекций, вход по GitHub PAT). До этого Task 1 (content/*.json обёрнуты в `{items:[...]}` под Sveltia file-коллекции). До этого ЛК Фаза 1 ЗАВЕРШЕНА (Task 1–6): Task 5 (блог(26)/истории(8)/кейсы(3)/песни(8) → `content/{blog,stories,cases,songs}.json` + загрузчик), Task 4 (товары (18) → `content/products.json` + загрузчик), Task 3 (scenarios/bundles/brands/static-pages + 13 `*Meta` → `content/*.json` + загрузчик), Task 2 (siteConfig+songsTotal → `content/site-config.json` + загрузчик), Task 1 Фазы 1 (тест-сеть безопасности контента `site.test.ts`), Task 6d дизайн-спринта «Сцена» (замыкающий фикс-коммит после финального адверсариального ревью), Task 6c (полировка a11y/perf — ДИЗАЙН-СПРИНТ «СЦЕНА» ЗАВЕРШЁН, Task 1–6). Ветка `main` — актуальная, деплоится на Cloudflare автоматически. Сборка чистая, тесты зелёные (web 29 + ui 2), CI/деплой зелёный.
>
> - **ЛК Фаза 2, Task 3:** страница /admin-guide/ (инструкция кабинета, noindex, «Сцена»), контент из admin-guide.md актуализирован под токен-вход; обе темы.
> - **ЛК Фаза 2, Task 2:** развёрнут Sveltia CMS на /admin (public/admin/index.html+config.yml), 11 file-коллекций на весь контент, вход по GitHub-токену (PAT), медиа → public/products; /admin грузится, config парсится.
> - **ЛК Фаза 2, Task 1:** файлы-массивы content/*.json обёрнуты в {items:[…]} под Sveltia file-коллекции, загрузчик site.ts читает .items; 29 тестов зелёные, сайт идентичен.
> - **ЛК Фаза 1 ЗАВЕРШЕНА (Task 1–6):** весь контент вынесен из site.ts в apps/web/content/*.json (11 файлов), site.ts = 169 строк чистого загрузчика+типов, тест контент-целостности (29 тестов), сайт визуально идентичен (обе темы проверены). Фундамент под CMS Фазы 2 (Sveltia /admin + /admin-guide). Инструкция владельца redaktirovanie-sajta.md актуализирована на content/*.json.
> - **Task 6d (замыкающий фикс) выполнен:** введены семантические токены `--color-success`/`--color-danger` (AA обе темы), исправлен сломанный `text-destructive` на `/servis` и захардкоженные зелёные индикаторы (servis+товар) → на токены, `#25D366` остался только за WhatsApp; бейдж скидки 11px (каскад), заголовки блога на `font-display`. ДИЗАЙН-СПРИНТ «СЦЕНА» ЗАКРЫТ ЧИСТО.
> - **Task 6c (полировка+доки) выполнен — ДИЗАЙН-СПРИНТ «СЦЕНА» ЗАВЕРШЁН (Task 1–6):** LCP-priority фото товара, декоративный alt в карточках, DiscountBadge вынесен, nbsp в числах тикеров, мобильные CTA лендингов ≥44px, скелетон на bg-scene; анти-AI правила добавлены в CLAUDE.md, блок облика в HANDOFF перезаписан на «Сцену».
>
> 🎭 **Дизайн-спринт «Сцена» (2026-07-04 → 2026-07-06, ЗАВЕРШЁН ЧИСТО):** спек — [`superpowers/specs/2026-07-04-dizayn-sprint-stsena-design.md`](superpowers/specs/2026-07-04-dizayn-sprint-stsena-design.md), план — [`superpowers/plans/2026-07-04-dizayn-sprint-stsena.md`](superpowers/plans/2026-07-04-dizayn-sprint-stsena.md). Реализован Task 1–6 (+6a-d) через subagent-driven (10 коммитов на проде, каждый двухступенчато отревьюен, финальное адверсариальное ревью 6 агентами). Остаток второго прохода анти-AI (should/nice) — в [`superpowers/stsena-backlog.md`](superpowers/stsena-backlog.md).
>
> 🗺️ **Дорожная карта (решение владельца 2026-07-06):** порядок работ — **(A) Личный кабинет владельца** (заказан «редактировать всё самой»; сайт статический `output:export`, редактирование = вариант B: вынос контента из `site.ts` + CMS-слой/админка на Cloudflare; та же инфраструктура закроет «заявки не сохраняются» из аудита). **Фаза 1 (вынос контента в `apps/web/content/*.json`) ЗАВЕРШЕНА (Task 1–6, 2026-07-06)** — фундамент готов. **Фаза 2 (CMS-слой/визуальный `/admin`, Sveltia) — в работе:** Task 1 (обёртка `{items:[…]}`) и Task 2 (сам `/admin` — Sveltia CMS, 11 file-коллекций, вход по GitHub PAT) готовы; план на 4 задачи — [`superpowers/plans/2026-07-07-lichnyy-kabinet-faza2.md`](superpowers/plans/2026-07-07-lichnyy-kabinet-faza2.md), гайд владельца — [`docs/client/admin-guide.md`](client/admin-guide.md). Затем **вместе (B+C): второй проход анти-AI** (`stsena-backlog.md`: стат-гриды, повтор «иконка-в-кружке», симметрия hero) **+ закрытие аудит-дыр «для друзей»** (аналитика — Метрика-заглушка/GA4/GSC; юридика — политика/оферта/реквизиты РК). Аудит целиком — [«последняя сессия 2026-07-04»](#) выше в тексте и отчёты сессии.
>
> 🎨 **Текущий облик («Сцена», 2026-07-07 — полировка: подсветка-текст + Onest, структура спринта не тронута):** тема по умолчанию — тёмная. Тёмная база: страница `#0e131c`, карточки `#172130`. Акцент — неон-жёлтый: `cta`/`primary`/`accent` `#facc15` (тёмный текст `#2a2400` на кнопках), `hot` (скидки) `#ff5c8a`, `success` `#4ade80`, `danger` `#ff6b6b` (оставлены). Светлая тема: страница `#edf0f5`, карточки `#ffffff`, акцент — чернильно-синий: `cta`/`primary`/`accent` `#17233f` (белый текст на кнопках), `hot` `#d6296b`, `success` `#15803d`, `danger` `#c02637` (оставлены). Токены `--color-*`, флипаются по теме через `.dark`, все пары ≥ AA (посчитано WCAG-скриптом при рецвете). Заголовки — шрифт **Onest** (`--font-display`, был Unbounded до 2026-07-07). Фирменный приём — подсветка строки (`<HighlightLine>`/`.hl`) на ключевом слове заголовков и в nav — **с 2026-07-07 акцентный текст (`color: var(--color-cta)` + подчёркивание), не заливка-блок**; компонент `HighlightLine` — чистый SSR `<span>` без анимации/хуков. Секции без нумерации 01-04 (убрана по решению владельца 2026-07-07). Тени-«клей» убраны — разделение поверхностей через hairline `border border-border`, тень только у плавающих элементов (FAB/оверлеи/sticky). Единый радиус `rounded-xl` (шкала до 12px, `rounded-2xl/3xl` запрещены). Тикеры (`.ticker`) вместо рядов pill-чипов для фактов доверия. Крупные карточки-контейнеры везде на едином фоне `bg-background` (унифицировано 2026-07-07 — ранее часть CTA/стат-карточек была на `bg-surface`). Сток-фото удалён — карточки/фото без реального `image` показывают типографический плейсхолдер на `bg-scene`. Выдуманные отзывы/рейтинги товаров/логотипы клиентов скрыты под флагами `SHOW_UNVERIFIED_*` (сейчас `false`). Логотип-марка: тёмно-синий квадрат `#17233f` с тонким бордером `#2a3852` + жёлтый микрофон `#facc15` (Header + фавикон) — перекрашена под новую палитру 2026-07-07 (была фиолетовая `#7c5cff` + белый мик), фиксированный бренд-цвет, не флипается по теме. WhatsApp-CTA — зелёный `#25D366` (фиксированный, не токенизирован намеренно). **CountUp — по решению владельца снова используется в UI (2026-07-07):** блок доверия на главной (14+ лет · 200+ объектов · 60 000+ песен · 2 года гарантия) и оживлённые числа в hero `/dlya-doma` (60 000+ песен) и `/dlya-biznesa` (200+ проектов) — анимация 0→значение при появлении в вьюпорте, SSR отдаёт финал. **Сток-фото — по решению владельца временно возвращены (2026-07-07):** `apps/web/public/scenariy/{poyushchie,dom,biznes}.jpg` (восстановлены из git-истории, коммит `15e0e92^`) вместо `bg-scene`-плейсхолдеров на главной (hero + секция «Дом/бизнес») и на `/dlya-doma`, `/dlya-biznesa` — до появления реальных фото от заказчика.
> - **Рецвет палитры (2026-07-07):** акцент «Сцены» сменён по решению владельца — тёмная неон-жёлтый #facc15, светлая чернильно-синий #17233f (theme.css, обе темы AA). Структура «Сцены» не тронута.
> - **Перекраска лого-марки (2026-07-07):** марка (Header-бейдж + фавикон) переведена с фиолетовой `#7c5cff`+белый на тёмно-синий `#17233f`+жёлтый `#facc15` под новую палитру; добавлен тонкий бордер `#2a3852`. WhatsApp не тронут.
> - **Счётчики+фото (2026-07-07):** по решению владельца возвращены анимированные счётчики (CountUp) на главную и лендинги + стоковые фото (scenariy/*.jpg восстановлены из истории) вместо bg-scene плейсхолдеров — временно, до реальных фото. Структура «Сцены» иначе не тронута.
>
> 🔧 **Аудит сайта (2026-06-22):** [`audit-2026-06-22.md`](audit-2026-06-22.md) — оценки и находки по визуалу/UX/продажам/SEO/репо. План устранения с файлами и проверкой — [`fix-plan-2026-06-22.md`](fix-plan-2026-06-22.md) (точка входа для работ).
> 🚀 **Запуск домена (2026-07-02):** [`strategy/domain-launch.md`](strategy/domain-launch.md) — перенос `karaokeshop.kz` с Wix на Cloudflare без потери Google, GSC, аналитика, локальное SEO, SEO-селф-чек. 301-правила — в [`apps/web/public/_redirects`](../apps/web/public/_redirects) (заполнить старые URL перед cutover; роут домена в `wrangler.toml` добавлять ТОЛЬКО на момент привязки — иначе CI упадёт).
> 🧪 **QA-прогон (2026-06-25):** [`qa-2026-06-25.md`](qa-2026-06-25.md) — функциональный прогон LIVE, 7 находок исправлено (бюджет-плацебо, og.jpg, крошки, Метрика, форма checkout, санитайз корзины).

## Последняя сессия (2026-07-15) — инфра-воркфлоу: AGENTS.md-симлинк + ресёрч новинок ИИ

- **`AGENTS.md` → симлинк на `CLAUDE.md`** (корень репо): открытый стандарт «README для агентов» (agents.md, 60k+ проектов, 20+ инструментов — Cursor/Codex/Copilot/Gemini CLI/Zed). Claude Code читает `CLAUDE.md`, но симлинк даёт бесплатную совместимость с любым другим агентом, единый источник правды. У medlog свой полноценный `AGENTS.md` уже есть — не трогали.
- **Токен-гигиена (глобально):** в `~/.claude/CLAUDE.md` добавлено правило владельца «Краткость и токен-гигиена» (без преамбул/пересказов/славословия, не перечитывать только что правленое, батчить тул-коллы, `/clear` между задачами, allowlist пермишнов).
- **Deep-research новинок ИИ (июль 2026)** для усиления воркфлоу — результаты и план внедрения выданы в чат. Ключевое: официальный плагин-маркетплейс Claude Code (255 плагинов — `typescript-lsp`, `security-guidance`, `code-review`, `pr-review-toolkit`, `cloudflare`, `github`); `DeusData/codebase-memory-mcp` (код-граф, экономия токенов); `alibaba/open-code-review` (гибридное CI-ревью); для AI-фич в статик-сайт — Cloudflare AI Search (быв. AutoRAG) + NLWeb (это «вариант B», требует решения владельца, домен `.kz` пока на Wix). Правило модель-роутинга сохранено в память агента.
- **`codebase-memory-mcp` установлен** (`~/.local/bin`, v0.9.0, MIT): локальный код-граф репо, MCP-сервер зарегистрирован в `~/.claude/`, скил `codebase-memory` поставлен. Индекс монорепо: 1736 узлов/2490 рёбер за ~1с, `search_graph` возвращает точные места без grep. ⚠️ Активируется после перезапуска Claude Code (MCP + хуки PreToolUse/SessionStart). Инсталлятор попутно сконфигурил Codex/VS Code/Cursor и добавил PATH в `.zshrc`; откат — `codebase-memory-mcp uninstall`. В репо ничего не пишет (persistence off).
- **AI-консультант «Караоке-эксперт» — дизайн одобрен (2026-07-15), спека:** [`superpowers/specs/2026-07-15-ai-konsultant-design.md`](superpowers/specs/2026-07-15-ai-konsultant-design.md). Отдельный Worker `ai-consultant` (главный сайт остаётся статикой), движок Claude Haiku, RAG НЕ нужен (корпус мал, база знаний вшивается на билде). Фаза 3a (llms.txt + аудит JSON-LD, чистая статика, без блокеров) — в работе. Фаза 3b (чат-Worker + виджет) требует действий владельца: Anthropic API-ключ секретом в Cloudflare + доступ к CF-аккаунту + «в прод» после проверки русского качества. Следующий шаг: `writing-plans` под 3b.

## Последняя сессия (2026-07-14) — Security Review: заголовки, JSON-LD, зависимости, мёртвый redirect

- **Security-ревью всего сайта (статика, бэкенда нет) — найдено и починено 5 пунктов, одним коммитом:**
  1. **Прод не отдавал ни одного security-заголовка** (проверено curl'ом live). Добавлен `apps/web/public/_headers` (Cloudflare Workers Static Assets): CSP (`'unsafe-inline'` вынужден — статик-экспорт без nonce; внешние скрипты закрыты, Метрика `mc.yandex.ru` разрешена заранее), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`. ⚠️ Файл валидируется на `wrangler deploy` → после пуша проверить CI и `curl -I` прода.
  2. **JSON-LD инъекция-гигиена:** 20 вызовов `dangerouslySetInnerHTML={{ __html: JSON.stringify(...) }}` в 14 файлах заменены на компонент `<JsonLd>` (`src/components/JsonLd.tsx`, экранирует `<` → `<`): строка `</script>` из контента (его правит владелец через /admin) больше не может разорвать тег. Скрипт темы в `layout.tsx` не тронут.
  3. **`redirects()` в `next.config.mjs` был мёртв** (Next молча игнорирует его при `output: "export"`): `/karaoke/dlya-doma` отдавал на проде 200-дубль `/dlya-doma` и сидел в sitemap. Дубль исключён из `generateStaticParams` и `allPaths()` (sitemap), 301 перенесён в `public/_redirects`, мёртвый конфиг удалён. → **Ловушка №9 в CLAUDE.md.**
  4. **Уязвимые dev-зависимости:** happy-dom ^15 (critical, VM escape → RCE) → ^20.10.6; vitest ^2.1.8 → ^4.1.10 (оба workspace); vite прижат к ^6.4.3 (патч esbuild-advisory; выше нельзя — Storybook 8 держит vite ≤6), `@vitejs/plugin-react` → ^5.2.0. Поймана поломка vitest 4 (не парсит `.tsx` при `"jsx": "preserve"` из Next-tsconfig — та самая, что осталась «тестовым долгом» после параллельных правок 2026-07-11): защита `esbuild: { jsx: "automatic" }` в `apps/web/vitest.config.ts` + vite-pin. → **Ловушка №10.** Остаток `npm audit` — 6 moderate, все dev-only (postcss в next, uuid в Storybook), лечатся только breaking-апгрейдами: принятый риск.
  5. Мелочь: `window.open` в `CheckoutClient` получил `"noopener,noreferrer"`.
- **Проверено и признано безопасным (не трогалось):** санитайз корзины в `cart.tsx`, все wa.me-ссылки через `encodeURIComponent`, все `target="_blank"` с `rel="noopener"`, скрипт темы статичен, Метрика под regex-гардом, изображения локальные.
- Сборка чистая, тесты зелёные: **web 29 + ui 2** (обе сюиты на vitest 4).
- **Примечание:** незакоммиченные «параллельные правки», упомянутые в записи 2026-07-11 (JsonLd, _headers, vitest 4), в репо не сохранились — работа выполнена заново с нуля и доведена (включая падавший тогда vitest 4).
- **Figma-экспорт (2026-07-14):** добавлен `packages/tokens/figma-tokens.json` — экспорт токенов в формате Tokens Studio (сеты global/light/dark + темы Light/Dark; radius, spacing, brand-цвета, типографика Onest/Manrope, 20 цветов обеих тем из theme.css). ⚠️ Файл производный: источник правды остаётся `theme.css`/`tokens.json` — при изменении токенов обновлять и его. Импорт: плагин «Tokens Studio for Figma» → Settings → Import file. Прямая сборка дизайн-системы в Figma-файле начиналась, но **отменена владельцем** (Figma MCP оказался подключён не к тому аккаунту — Zauren Zhambylova); пустой черновик «Karaokeshop — дизайн-система и страницы» в drafts того аккаунта можно удалить вручную. Спека сборки сохранена в scratchpad сессии; ⚠️ **Запрет владельца: НИКОГДА не подключаться к Figma-аккаунту Zauren Zhambylova и ничего там не делать** (ловушка 11 в CLAUDE.md, продублировано глобально в ~/.claude/CLAUDE.md).
- **Партия C выполнена (Opus + добор Fable): 10 UX-фиксов пути клиента.** C1: на мобильном товаре появилась «В корзину» (в `ProductStickyBar`, через `AddToCart compact` 44×44; после добавления — ссылка «В корзине → оформление»; WhatsApp остался единственным зелёным CTA). C2: каталог читает `?q=` (`useSearchParams` + Suspense, страница осталась статической). C3: поиск находит товары по типу и note («микрофон» → 6 товаров, было 0). C4: checkout открывает WhatsApp ДО очистки корзины. C5: в результате квиза ссылка «Посмотреть системы в каталоге». C6: мёртвая «Нужна консультация» стала нейтральным ghost-анкором на WhatsApp-консультацию. C7: телефон в мобильном меню кликабелен (tel:). C8: фото товара на мобильном 4:3 вместо квадрата (плюс добор: `minHeight:360` контейнера переведён в `lg:min-h-[360px]`, иначе перекрывал aspect). C9: «—» в подсказках сметы (`lib/calculator.ts`) заменены с вариацией. C10: чипы фильтров каталога ≥40px. Всё проверено прогоном на превью 375px.
- **Партия B выполнена (3 Opus-агента параллельно):** 23 файла мигрированы на компоненты партии A, −271 строка чистыми (217+/488−). Главная: инлайн-карточка → `ProductCard size="lg"`; лендинги/контентные страницы: `WaButton`/`CtaSection`/`FaqBlock`/`Breadcrumb withLd`; сырых сборок `wa.me` в коде 0 (все через `waHref`). Осознанные исключения: dlya-doma/keysy/sravnenie оставили ручной breadcrumb-JSON-LD (имена в LD и видимой крошке различаются, `withLd` молча изменил бы структурированные данные), servis/o-nas финальные CTA уникальны. Тесты 32 зелёные.
- **Партия A выполнена (Opus):** созданы `lib/wa.ts`(+тест), `WaButton`, `SectionHeader`+`MobileActionLink`, `CtaSection`, `FaqBlock`, `OptionButton`, `Breadcrumb withLd` — пока никем не используются (миграция = партия B). Тесты web: 32.
- **План сокращения кода + путь клиента (эта же сессия, продолжение):** составлен план [`superpowers/plans/2026-07-14-sokrashchenie-koda.md`](superpowers/plans/2026-07-14-sokrashchenie-koda.md) — партия A (фундамент: `wa.ts`, `WaButton`, `CtaSection`, `FaqBlock`, `OptionButton`, `SectionHeader`, `Breadcrumb withLd`), партия B (миграция страниц, 3 агента), партия C (UX-фиксы по прогону пути клиента на 375px). Ключевые находки прогона: **на мобильном товаре нет «В корзину»** (CTA-блок `hidden lg:flex`, sticky-бар только WhatsApp — корзина недостижима с телефона), поиск обрывается на Enter (`/catalog?q=` игнорируется), поиск не находит товары по типу («микрофон» → 0 товаров), checkout чистит корзину до открытия WhatsApp, мёртвая кнопка «Нужна консультация», телефон в мобильном меню некликабелен, остатки «—» в подсказках калькулятора (`lib/components.ts`).

## Последняя сессия (2026-07-11) — анти-AI: убрано длинное тире «—» из видимой прозы (заход 1)

- **Новое анти-AI правило владельца (2026-07-11): длинное тире «—» = AI-телл, убрать из видимой прозы.** Закреплено правилом №10 в `CLAUDE.md` (раздел «Анти-AI правила») + личная память. Замена **варьируется** (запятая / точка / двоеточие / переформулировка в актив), чтобы двоеточие само не стало теллом. Короткое тире «–» в диапазонах (`20–60`, `1–2 дня`, `70–90 °C`) и дефис в словах (`Караоке-вечер`, `AST-250`) НЕ трогаются.
- **Заход 1 — публичный фронт:** убраны ВСЕ видимые «—» на главной (`page.tsx`, 11 мест), лендингах `dlya-doma` (8), `dlya-biznesa` (8), странице `o-nas` (5), `gotovye-resheniya` (2). Код-комментарии `{/* … */}` и фейк-отзыв под флагом `SHOW_UNVERIFIED_SOCIAL_PROOF=false` не трогались (не видны на проде).
- **Заход 2 — остальные публичные страницы + мелкий контент-JSON (владелец выбрал «всё, включая блог»):** зачищены `servis`, `sravnenie`, `pod-klyuch`, `kontakty`, `keysy`, `product/[slug]`, `brand/[slug]`, `CalculatorClient` (шаги «Шаг N.»), `QuizWidget`, `CheckoutClient` + JSON `page-meta` (9, попутно убран второй задвоенный бренд в data-title `dlya-biznesa`), `scenarios` (5), `bundles` (4), `brands` (2), `cases` (1), `site-config` (1), `stories` (8 историй переписаны целиком). Замена варьируется (запятая/точка/двоеточие/«?»/актив). `komplekty/[area]` и `MobileNav`/`ProductImage`/`layout` — только код-комментарии, не трогались.
- **Сознательно оставлены (НЕ прозаические теллы):** WhatsApp-буллеты `— ` в генерируемых сообщениях (`QuizWidget` строки 57–59, `CheckoutClient` строка 53) — это маркеры списка в тексте, уходящем в WhatsApp, не он-страничная проза. Если владелец захочет — заменим на `•`/дефис отдельным решением.
- **Заход 3 — блог + добор (ЗАВЕРШЕНО):** `blog.json` (370 вхождений «—» в 26 статьях) зачищен текстовым проходом « — » → «, » (em-dash с пробелами встречается только внутри JSON-строк, синтаксис не задет; дифф ровно 217 строк, JSON валиден). Проверено на splice'ы: «запятая+Заглавная» — только имена собственные (Алматы/Астана/Димаш/DHL), run-on'ов нет; тире в этом контенте работало как приложение/вводная пауза, запятая читается естественно. Одно определение (стр. 28) доведено до двоеточия. Плюс добор 4 пропущенных видимых мест: `blog/page.tsx`, `kalkulyator/page.tsx`, `AreaCalculator.tsx`, `CatalogClient.tsx`.
- **ИТОГ по «—»: в видимой прозе 0** (контент-JSON 0, все страницы/компоненты 0). Осталось только НЕвидимое: код-комментарии `//`/`{/* */}`, `admin-guide` (noindex, 21), фейк-отзыв под флагом, WhatsApp-буллеты `— ` в генерируемых сообщениях (`QuizWidget`/`CheckoutClient`).
- **⚠️ Параллельные правки в рабочем дереве (НЕ мои, не закоммичены):** во время зачистки в репо появились чужие незакоммиченные изменения — новый компонент `JsonLd.tsx` + рефактор JSON-LD по страницам, `next.config.mjs`, `_headers`, и апгрейд `vitest ^2.1.8 → ^4.1.10` в `apps/web/package.json`. **vitest 4 ломает `npm test`** (не парсит `CountUp.tsx`, ошибка JSX-конфига; Next-сборка при этом зелёная — код валиден, дело в vite/jsx-транформе новой версии). Я коммитил **только свои 5 файлов** (blog.json + 4), `git add -A` не делал. Тестовый долг vitest 4 — за владельцем/следующей сессией.

## Последняя сессия (2026-07-10) — правки заголовков: hero главной + гео /dlya-biznesa

- **Прогон stop-slop по видимой прозе (2026-07-10):** применены правила skill hardikpandya/stop-slop (анти-AI-теллы), адаптированные под русский (НЕ трогаю em-dash — в RU тире норма; НЕ трогаю «Wh-» — англо-специфично). Убран пустой копирайт-слоп в 6 видимых местах: `o-nas` («Наша специализация — индивидуальный подход» → сразу конкретика действий), `dlya-biznesa` карточки («идеальный баланс цены и качества» → «мощности хватает на зал до 80 м², без переплаты»; «высокое качество звука» → «гости слышат одинаково за любым столом»), `gotovye-resheniya` CTA («подберём индивидуально» → «подберём комплект»), два мета-описания в `page-meta.json` (home: «Индивидуальный подход» → «Расчёт под площадь и бюджет»; sravnenie: «подберём индивидуально» → «под вашу задачу и бюджет»). Фейк-отзывы (`page.tsx:57`) не трогал — под флагом `SHOW_UNVERIFIED_SOCIAL_PROOF=false`, не видны. Блог оставлен как есть — вхождения «идеально/индивидуально» там контекстно-конкретны (напр. «20–60 человек — идеальный размер»).
- **Новое операционное правило владельца (2026-07-10): «что и почему ломается — обязательно записывать, чтобы не повторять».** Закреплено в шапке раздела «Ловушки» в `CLAUDE.md` (поймал поломку → разбери корневую причину → добавь пункт-ловушку в том же коммите) и в личной памяти агента. Первое применение — ловушка №8 ниже.

- **`/dlya-biznesa` H1: «под ключ — Алматы» → «под ключ — Алматы и по Казахстану» (2026-07-10):** по правке владельца («работаем не только в Алматы, но по Казахстану»). Остальная страница уже говорила про весь Казахстан (whatsIncluded «Выезд в любой город Казахстана», FAQ «по всему Казахстану: Астана, Шымкент…») — H1 был единственным «только Алматы». Вариант выбран additive: Алматы (локальное SEO) сохранён, Казахстан добавлен.
- **`/dlya-biznesa` SEO-title:** «…под ключ — Алматы | karaokeshop» → «…под ключ — **Алматы и Казахстан**» (ручной `| karaokeshop` убран). Попутно устранён **пре-existing баг задвоения бренда**: корневой layout (`app/layout.tsx:40`) уже добавляет `%s | karaokeshop` через `title.template`, а страница вписывала `| karaokeshop` руками → на LIVE было «…| karaokeshop | karaokeshop». Это была единственная страница в `src` с ручным брендом в title (проверено grep). Теперь бренд один.
- **Заголовок hero главной всегда в две строки «Караоке-вечер / у тебя дома» + убран артефакт подчёркивания под «у» (2026-07-10):** по правке владельца (скриншот).
- **`app/page.tsx` h1:** было `Караоке-вечер <HighlightLine>у тебя дома</HighlightLine>` — на узкой ширине span «у тебя дома» рвался по словам: «у» уезжало в конец 1-й строки к неподсвеченному тексту, span разрывался на две строки → (1) перенос получался после «у», а не после «вечер»; (2) фрагмент span «у» на 1-й строке получал свой короткий кусок `text-decoration: underline` = артефакт-чёрточка под «у». Стало: `Караоке-вечер<br/><HighlightLine className="whitespace-nowrap">у тебя дома</HighlightLine>` — форсированный перенос после «вечер» + запрет внутреннего разрыва span → на любом устройстве ровно две строки, всё подчёркивание «у тебя дома» цельной чистой линией. Один источник (главная), лендинги не затронуты.
- **Проверки:** `npm run build -w web` чист, `npm test -w web` 29/29. HTML: главная `Караоке-вечер<br/><span class="hl whitespace-nowrap">у тебя дома</span>`; `/dlya-biznesa` H1 «под ключ — Алматы и по Казахстану», title `…Алматы и Казахстан | karaokeshop` (один бренд). Превью-MCP привязан к medlog → визуал сверял по эмитированному HTML, после пуша — `curl` живого URL. Hero главной подтверждён на LIVE (CI зелёный).

## Последняя сессия (2026-07-08) — hero-фото выровнено с заголовком

- **Hero-фото выровнено (2026-07-08):** фото справа тянется от заголовка до низа квиза (absolute inset-0 в растянутой колонке), убрана пустота под квизом; заголовок/квиз в левой колонке.
- **`app/page.tsx` hero-секция:** тикер/`h1`/подзаголовок/`QuizWidget` перенесены внутрь `lg:col-span-7` (левая колонка), сетка получила `lg:items-stretch`. Правая колонка-обёртка фото (`lg:col-span-5`) стала `relative` + `min-h-[280px]`, сама `<img>` — `absolute inset-0 h-full w-full object-cover object-[center_25%]` (было `hidden self-stretch` обёртка + `h-full w-full` картинка без absolute — из-за чего колонка с фото не тянулась вровень с левой, и под квизом оставалась пустота, а фото торчало ниже). Проверено в превью (замер `getBoundingClientRect`): верх фото совпадает с верхом левой колонки (diff 0px), низ фото совпадает с низом квиза (diff 0px) — в обеих темах. На `md`/планшете (`<lg`) фото по-прежнему скрыто (`hidden lg:block`), пустоты не образует.

## Последняя сессия (2026-07-07) — меньше пустоты сверху/по бокам

- **Меньше пустоты сверху/по бокам (2026-07-07):** Container расширен max-w-6xl→max-w-7xl (меньше полей по бокам, глобально; проза блога ограничена для читаемости), верх главной поднят (py и hero pt уменьшены).
- **`Container.tsx`:** `max-w-6xl` (1152px) → `max-w-7xl` (1280px), `px-4` не тронут. Общий компонент — контент на всех страницах стал шире на 128px. Блог-статья (`app/blog/[slug]/page.tsx`) не пострадала — `<article>` там уже был на собственном `mx-auto max-w-2xl` (672px), независимом от `Container`; проверено в превью, строки текста статьи остались читаемыми (не растянулись).
- **`app/page.tsx` (главная):** `<Container className="py-6 sm:py-10">` → `py-3 sm:py-5`; hero `<section className="animate-fade-up pt-2 lg:pt-6">` → `pt-0 lg:pt-2`. Зазор шапка→тикер сократился с 64px до 28px.
- **Проверки:** `npm run build -w web` чист, `npm test -w web` 29/29, `npm test -w @kk/ui` 2/2. Превью (`karaoke-static` :4321) обе темы: контент 1280px (было 1152px), зазор header→тикер 28px (было 64px), блог-статья читаема (672px, отдельный max-w). Тема возвращена в дефолт (тёмная).

## Предыдущая сессия (2026-07-07) — главная: подтянуты вертикальные отступы

- **Подтянуты отступы главной (2026-07-07):** по «убери пустые места» уменьшены верхний отступ hero, внутренние gap и зазоры между секциями (mt-16→mt-10, mt-12→mt-8 и т.п.) — плотнее, без потери читаемости.

## Последняя сессия (2026-07-07) — дизайн-стандарт: docs/design/visualization/ (переносимый) + ссылки в CLAUDE.md

- **Добавлен переносимый стандарт визуализации** в [`docs/design/visualization/`](design/visualization/): `VISUALIZATION.md` (правило — роль-токены `--color-*`, дисциплина нейтралей, контраст WCAG, тёмная тема, типографика/сетка, анти-ИИ, процесс, DoD + вшитая JS-функция контраста), `color-systems-guide.md` (принципы/established-системы/палитры с hex + источники), `palette-medlog.md` (готовая палитра MedLog под пациента, light+dark), `check.mjs` (контраст-чекер WCAG, `node check.mjs` — проверен), `session-handoff.md` (паст-ин для подключения стандарта в новом проекте), `README.md` (индекс). Универсальный, цвето-агностичный — конкретные цвета подбираются локально; правила «Сцены» — частная реализация.
- **`CLAUDE.md`** — в начало раздела «UI-конвенции» добавлена врезка со ссылкой на стандарт. **`docs/design/README.md`** — добавлен пункт про `visualization/`.
- Мастер-копия набора — вне репо, в `~/Desktop/design-docs/` (раздаётся всем проектам владельца).
- Только доки: код сайта, токены `theme.css`, сборка и тесты не затронуты.

## Последняя сессия (2026-07-07) — полировка облика: подсветка-текст, шрифт Onest, без нумерации, единые карточки, фото hero

- **Полировка облика (2026-07-07):** по правкам владельца — подсветка заголовков стала акцентным текстом с тонким подчёркиванием (вместо жёлтого блока, меньше агрессии), шрифт заголовков Unbounded→Onest (проще), убрана нумерация секций 01-04, все карточки единый фон bg-background, фото hero выровнено.
- **1) `.hl` — блок → акцентный текст:** `app/globals.css` — вместо заливки `background-image`/`background-size`-анимации (`.hl-armed`/`.hl-on` + `prefers-reduced-motion`) теперь просто `color: var(--color-cta)` + `font-weight: 700` + `text-decoration: underline` (`text-decoration-color: var(--color-cta)`, толщина `0.07em`, `text-underline-offset: 0.12em`). Тёмная тема — неон-жёлтый текст, светлая — чернильно-синий текст с подчёркиванием (сам цвет ≈ цвет заголовка, подчёркивание — единственный дифференциатор, но читается уверенно: инспектор показал `text-decoration-thickness: 4.2px` на h1). `HighlightLine.tsx` упрощён — убраны `useEffect`/`IntersectionObserver`/`useState`/`"use client"`, теперь чистый SSR-компонент `<span className="hl">`. Побочный эффект (желаемый): активный пункт nav (Header/MobileNav) и активная вкладка каталога (`CatalogClient`) тоже используют `.hl` — были жёлтой плашкой, стали акцентным текстом с подчёркиванием; проверено в превью обеих тем — читается чётко.
- **2) Шрифт заголовков:** `app/layout.tsx` — `Unbounded` → `Onest` (`next/font/google`, `subsets: ["latin","cyrillic"]`, `weight: ["700","800"]`, та же переменная `--font-display`). Manrope (body) не тронут, класс `font-display` в компонентах не менялся.
- **3) Нумерация секций 01-04 убрана:** `app/page.tsx` — `SectionTitle` больше не принимает/не рендерит проп `num` (убран `<span className="text-primary/40">{num}</span>`); секция «Как мы работаем» — шаги без `{s.step}` (поле `step` убрано из данных, остались `title`+`body`). Grep `SectionTitle|text-primary/40|num="0` по `apps/web/src` после правки — нумерация была только на главной (`page.tsx`), на других страницах/лендингах её не было (единственное совпадение `text-primary/40` вне page.tsx — `ProductImage.tsx`, это цвет текста типографического плейсхолдера модели товара, не номер секции — не тронут).
- **4) Единый фон карточек:** grep `bg-surface|bg-muted|bg-gradient|from-\[` по `app` и `components` → крупные карточки-контейнеры (CTA-блоки, стат-карточки с иконкой/числом, hero-карточки) на `bg-surface` приведены к `bg-background border border-border` (единый фон карточек в каждой теме). Затронуто: `dlya-doma`, `dlya-biznesa`, `admin-guide` (CTA-карточка), `kontakty` («Быстрые запросы»), `pod-klyuch` (стат-карточки доверия + CTA), `keysy` (CTA), `sravnenie` (CTA), `pesni` (3 стат-карточки), `gotovye-resheniya` (CTA), `servis` (CTA), `o-nas` (hero-карточка + 4 стат-карточки). Главная (`page.tsx`) уже была на `bg-background` — не требовала правки. НЕ трогали: мелкие пилюли/чипы (`rounded-full bg-surface` — характеристики товара, теги), инлайн-`<code>` подсказки и мелкие заметки (`admin-guide`), скелетоны (`bg-muted` в `CatalogClient`), hover-состояния (`hover:bg-surface`/`hover:bg-muted` в `AreaCalculator`/`BrandProductFilter`/`MobileNav`/`SearchOverlay`/`SongsClient`/`BundleTiers`), `Footer` (фон подвала — не карточка), `ScrollToTop`/`CheckoutClient` (плавающая кнопка/маленький иконка-бейдж), таблица `sravnenie` (шапка/зебра строк — не карточки). Проверено JS-обходом в превью: все элементы `.bg-background` внутри контента резолвятся в один и тот же RGB (кроме `Header` — намеренно другой токен `dark:bg-page` для sticky-шапки, вне карточек).
- **5) Фото hero выровнено:** `app/page.tsx` hero — контейнеру фото (`lg:col-span-5`) добавлены `self-stretch` (явно, вместо неявного grid-stretch) и `border border-border` (hairline, как у остальных карточек-фото); изображению добавлен `object-[center_25%]`, чтобы лица не обрезались при высоте, заданной квизом (~679px). Превью: фото больше не выглядит «поехавшим», лица видны, выравнивание с квизом сохранено в обеих темах.
- **Проверки:** `npm run build -w web` чист (статика в `apps/web/out`, `check-redirects` ок), `npm test -w web` 29/29, `npm test -w @kk/ui` 2/2. Превью (`karaoke-static` :4321) обе темы — заголовок «у тебя дома» акцентным текстом с подчёркиванием (не блок), Onest подтверждён инспектором (`font-family: Onest, ...`), секции без номеров (JS-проверка: 0 `span` с текстом `01`-`09` на странице), карточки единого фона, активный nav/вкладка — акцентный текст, фото hero выровнено. Тема возвращена в дефолт (тёмная).

## Предыдущая сессия (2026-07-07) — возврат счётчиков (CountUp) + стоковых фото (временно)

- **Контекст:** по решению владельца возвращены два элемента, ранее убранные дизайн-спринтом «Сцена»: анимированные счётчики (`CountUp`, компонент уже был в репо — только не использовался в UI) и стоковые фото (временно, до реальных). Остальная структура «Сцены» (тикеры, hairline-карточки, палитра неон-жёлтый/чернильно-синий) не тронута.
- **Фото восстановлены из git-истории:** `git checkout 15e0e92^ -- apps/web/public/scenariy/` вернул `poyushchie.jpg` (270 КБ), `dom.jpg` (259 КБ), `biznes.jpg` (290 КБ) — коммит `15e0e92` их удалил при миграции на токены «Сцены» 2026-07-05.
- **Главная (`app/page.tsx`):** hero-плашка `bg-scene`+`AST · EVOBOX` → `<img src="/scenariy/poyushchie.jpg">` (справа от квиза, `lg:` онли, `object-cover rounded-xl`, `loading="eager" fetchPriority="high"`). Секция «Дом/бизнес»: `plate`-текст в данных заменён на поле `image` (`/scenariy/dom.jpg`, `/scenariy/biznes.jpg`), рендер — `<img object-cover>` вместо `bg-scene`+`<span>` (высоты `h-40 sm:h-48` сохранены). Добавлен новый блок доверия после hero — 4 счётчика `CountUp` (`14+` лет на рынке · `200+` объектов · `60 000+` песен · `2 года` гарантия), hairline `border-y border-border`, число `text-primary`, подпись `text-muted-foreground`.
- **Лендинги:** `/dlya-doma` — hero-плашка → `<img src="/scenariy/dom.jpg">` (aspect-[4/3] сохранён), фраза «60 000+ песен» в тикере оживлена через `<CountUp value="60 000+ песен" />`. `/dlya-biznesa` — hero-плашка → `<img src="/scenariy/biznes.jpg">`, «200+» в hero-прозе («…персонал. 200+ проектов с 2012 года.») оживлено через `<CountUp value="200+" />` (тикер под кнопкой оставлен статичным текстом, как в задании).
- **Проверки:** `npm run build -w web` чист (фото копируются в `out/scenariy/`), `npm test -w web` 29/29, `npm test -w @kk/ui` 2/2. Превью (`karaoke-static` :4321) обе темы: hero-фото и секция «Дом/бизнес» на месте (network 200 на все три `.jpg`), счётчики реально анимируются (inspect ловил промежуточные кадры вроде «1 316+ песен» → «60 000+»), вёрстка не поехала. Тема возвращена в дефолт (тёмная).
- **Не тронуто:** `CountUp.tsx` (логика/parseValue не менялись), палитра/токены, тикеры, hairline-приёмы, данные `site.ts` (кроме локального `image`-поля в массиве «Дом/бизнес» внутри `page.tsx`).

## Предыдущая сессия (2026-07-07) — лого-марка перекрашена под новую палитру

- **Header.tsx (бейдж-марка):** фон квадрата `#7c5cff` → `#17233f` (тёмно-синий), микрофон (`lucide Mic2`, был белый через `text-white`) → `#facc15` (жёлтый, инлайн `style` на иконке), добавлен тонкий бордер `1px solid #2a3852` (`box-border`, чтобы не съедал 28×28 размер квадрата) — чтобы марка не сливалась с тёмным хедером `#0e131c`. Вордмарк `karaokeshop` и остальной Header не тронуты.
- **`app/icon.svg` (фавикон):** фон `#7c5cff` → `#17233f`, микрофон (тело+стойка) `#ffffff` → `#facc15`. Форма/скругление (`rx=8`) не менялись.
- Марка фиксированная (не флипается по теме) — синий+жёлтый в обеих темах, как раньше был фиолетовый+белый во всех темах.
- **Доки:** `docs/HANDOFF.md` (этот файл — блок «Текущий облик») и `docs/design/design-system.md` (раздел «Марка · акцент») синхронизированы на новый HEX марки; исторические записи прошлых сессий и `docs/superpowers/plans/2026-07-04-dizayn-sprint-stsena.md`/`specs/2026-07-04-dizayn-sprint-stsena-design.md` (зафиксированный на дату спринта план/спек) намеренно не переписаны задним числом.
- **Проверки:** `npm run build -w web` чист, `npm test -w web` 29/29, `npm test -w @kk/ui` 2/2. Превью (`karaoke-static` :4321) обеих тем — марка читается на тёмном и светлом хедере. Тема возвращена в дефолт (тёмная).

## Предыдущая сессия (2026-07-07) — рецвет палитры «Сцены»: неон-жёлтый (тёмная) / чернильно-синий (светлая)

- **Только цвета в `packages/tokens/css/theme.css` (единый источник) — структура/приёмы «Сцены» не тронуты.** По решению владельца акцент сменён: тёмная тема (по умолчанию) — неон-жёлтый `#facc15` (`primary`/`accent`/`cta`, тёмный текст `#2a2400` на кнопках), светлая — чернильно-синий `#17233f` (белый текст на кнопках). Обе базы страниц/карточек тоже обновлены под холодную гамму: тёмная `page #0e131c`/`bg #172130`/`surface #22304a`, светлая `page #edf0f5`/`bg #ffffff`/`surface #e4e9f0`. `hot`/`success`/`danger` оставлены как были.
- **Контраст пересчитан WCAG-скриптом (node), все пары ≥ AA:** тёмная — fg/bg 14.69:1, muted-fg/bg 8.65:1, primary/bg 10.58:1, cta-fg/cta 10.15:1, accent-fg/accent-soft 13.93:1 (реальная пара использования, не accent-fg/accent — тот вариант даёт 1.38:1 и в коде не встречается); светлая — fg/bg 16.13:1, muted-fg/page 5.44:1, primary/bg 15.57:1, cta-fg/cta 15.57:1. Комментарии-контрасты в `theme.css` обновлены на фактические числа.
- **Grep остаточного фиолета вне theme.css — 0 хитов** (кроме лого-марки `#7c5cff` в `Header.tsx`/`icon.svg`, которая намеренно не тронута). Хардкод `text-[#/bg-[#/from-[#/to-[#` в `apps/web/src` — только WhatsApp `#25D366`/`#1ebe5d`, тоже не тронут.
- **Проверки:** `npm run build -w web` чист, `npm test -w web` 29/29, `npm test -w @kk/ui` 2/2. Превью (`karaoke-static` :4321) обеих тем на `/` и `/catalog` — тёмная: страница `#0e131c`, кнопки/подсветка/цена неон-жёлтые с тёмным текстом, карточки `#172130` с hairline; светлая: бумага `#edf0f5`, акцент чернильно-синий с белым текстом, карточки белые. Тема возвращена в дефолт (тёмная, `localStorage` очищен).
- **Синхронизирован `docs/design/design-system.md`** (шпаргалка дизайн-системы, создана в предыдущей сессии в этот же день) — таблицы токенов, промпты Google Stitch (EN/RU) и UI-панель Stitch переведены на новую палитру.

## Предыдущая сессия (2026-07-07) — доки: шпаргалка дизайн-системы «Сцена» (Figma/Stitch)

- **Только доки, кода/данных не касалось.** Создан [`docs/design/design-system.md`](design/design-system.md) — канонический справочник дизайн-системы «Сцена» под перенос в Figma/Google Stitch: все токены цвета обеих тем (light + dark) с HEX-парами (поверхности/текст/марка-акцент/действия/состояния/сцена, 20 токенов + марка `#7c5cff`), типографика (Unbounded 600/700 + Manrope 400–700, шкала `text-xs`…`text-6xl` в px/line-height), радиусы 6/8/10/12/full (+ запрет `2xl/3xl`), шкала отступов 4→96px, сетка (container 1152/px-16), правила «Сцены». Значения взяты 1:1 из `theme.css`/`tokens.json`/`tailwind.config.ts`/`layout.tsx` — файл помечен «источник правды — код, не этот файл».
- **Плюс готовые промпты для Google Stitch (тёмная тема) — на генерацию UI-кита, а не лендинга:** EN + RU, оформлены как экран-витрина компонентов (7 секций: цвета/типографика/кнопки/бейджи/поля/карточки/радиусы-отступы), триггер «UI Kit / Style Guide screen (component sheet, NOT an app page)» + «reusable component library sheet for handoff to Figma». Плюс таблица маппинга на UI-панель Stitch (Mode=Dark, Primary `#7250F8`, Background `#0B0913`, Font Manrope/Unbounded, radius 8px).
- Индексы обновлены: пункт в [`docs/README.md`](README.md) (Ключевые документы) и в [`docs/design/README.md`](design/README.md) (раздел помечен ✅). Ранее файл `design-system.md` числился в планах папки — теперь реализован.
- Также в чате отдан визуальный Artifact-лист (HTML-шпаргалка с копированием HEX по клику) — вне репо, живёт в claude.ai.

## Позапрошлая сессия (2026-07-07) — ЛК Фаза 2, Task 3: страница /admin-guide/

- **ЛК Фаза 2, Task 3:** страница-инструкция `apps/web/src/app/admin-guide/page.tsx` — серверный компонент, `robots: { index: false, follow: false }` (подтверждено в собранном `admin-guide.html`: `<meta name="robots" content="noindex, nofollow"/>`), не участвует в sitemap (`allPaths()` в `site.ts` — явный список путей, `/admin-guide` в нём не появляется — исключать нечего). Разметка на компонентах «Сцены»: `<Container>` → `<Breadcrumb items={[{ label: "Инструкция кабинета" }]} />` → `<h1 className="font-display …">` с `<HighlightLine>` на слове «кабинет» → секции-карточки `rounded-xl border border-border bg-background` (hairline, без теней) с иконками `lucide-react` (guard `{Icon && …}` — поле `icon` опционально) → кнопка `<a href="/admin/" className="… rounded-lg bg-cta … text-cta-fg">Открыть кабинет</a>` (дублирована вверху и внизу страницы, обе ≥44px по high). Контент — 8 секций из `docs/client/admin-guide.md` (что где / цена / фото / блог / после сохранения / чего не делать / если не появилось), плюс раздел «Как зайти» нумерованным списком.
  - **Вход актуализирован под токен (было под OAuth-«Войти через GitHub»/Authorize — так уже не работает, Task 2 развернул PAT-вход Sveltia).** Новый текст — и на странице, и в `docs/client/admin-guide.md` (раздел «Как зайти» переписан идентично по сути): открыть `/admin/` → «Sign In Using Access Token» → создать fine-grained PAT (Repository access → только `airginggger-collab/Karaokeshop`, Permissions → Contents: Read and write, срок по желанию) → вставить токен → токен сохраняется в браузере, повторный ввод только при смене браузера/устройства.
  - `docs/client/admin-guide.md` также: шапка-сноска обновлена (кабинет уже работает, а не «в разработке»), раздел «Пока кабинет не готов (Фаза 1)» заменён на короткую заметку «Кабинет уже работает» (визуальный редактор развёрнут, JSON-правка через GitHub остаётся резервным способом, ссылка на `redaktirovanie-sajta.md` сохранена).
  - Build чистый (`npm run build -w web` — новый статический роут `/admin-guide` в выводе, postbuild check-redirects OK), `npm test -w web` = 29/29, `npm test -w @kk/ui` = 2/2 (страница не трогает данные/тесты). Превью (`karaoke-static`, порт 4321) в обеих темах: крошки «Главная › Инструкция кабинета» на месте, h1 с подсветкой слова «кабинет» (заливка фиолетовым в обеих темах), текст читаем на `text-muted-foreground`/`text-foreground`, обе кнопки «Открыть кабинет» ведут на `/admin/` (проверено `document.querySelectorAll('a[href="/admin/"]').length === 2`), светлая/тёмная — визуально идентичны по структуре, тёмная возвращена дефолтом после проверки.
- **ЛК Фаза 2, Task 2:** развёрнут Sveltia CMS на /admin (public/admin/index.html+config.yml), 11 file-коллекций на весь контент, вход по GitHub-токену (PAT), медиа → public/products; /admin грузится, config парсится. Два новых файла: `apps/web/public/admin/index.html` (грузит `https://unpkg.com/@sveltia/cms/dist/sveltia-cms.js` — CDN-URL сверен с офиц. доками Getting Started) и `apps/web/public/admin/config.yml` (`backend: {name: github, repo: airginggger-collab/Karaokeshop, branch: main}` — PAT-вход, `base_url` не нужен; `media_folder: apps/web/public/products`, `public_folder: /products`). 11 file-коллекций — каждая на один файл `apps/web/content/*.json`: 9 массивов (products/blog/stories/cases/songs/scenarios/bundles/brands/static-pages) через `items` виджета `list` с вложенными `fields`, точь-в-точь под типы `Product/BlogPost/StoryPost/Case/Song/Landing/Brand` из `site.ts` (опциональные поля типа → `required: false`); 2 объекта (`site-config.json` — 11 полей напрямую, `page-meta.json` — 13 полей `object` с вложенными slug/h1/title/description). Простые списки строк (`kit`, `tags`, `body`-абзацы) — `widget: list` + `field:` (единственное число, без `name` в выводе) вместо `fields:`. Проверено в превью: `/admin/` грузит SPA, экран входа Sveltia (Sign In with GitHub / Sign In Using Access Token), 0 ошибок в консоли, 0 упавших сетевых запросов — конфиг распарсился чисто (репо в тексте экрана — «Karaokeshop»). Build чистый, `npm test -w web` 29/29, `npm test -w @kk/ui` 2/2.
  - **Расхождения со сценарием задачи, найденные по докам Sveltia (WebFetch, дата проверки 2026-07-07):** (1) `summary` внутри `list`-виджета шаблонизируется как `{{model}}`, БЕЗ префикса `fields.` — в задаче было `{{fields.model}} — {{fields.price}} ₸`, использовал верный `{{model}} — {{price}} ₸`. (2) file-коллекция оборачивается как `collections: [{name, label, files: [{name, label, file, fields}]}]` — не голый `{name, label, file, fields}` на верхнем уровне коллекции. (3) `required: false` подтверждено как штатный флаг «необязательное поле» — единственное отличие Sveltia от Decap здесь косметическое (UI помечает бейджем required-поля, а не optional, как в Decap); семантика YAML-ключа не меняется.
- **ЛК Фаза 2, Task 1:** Sveltia CMS требует у file-коллекции JSON-объект в корне (не голый массив) — 9 файлов-массивов в `apps/web/content/` обёрнуты в `{ "items": [...] }` скриптом (`node -e` per-файл, verbatim-перенос элементов, счётчики сверены до/после): `products.json` (18), `blog.json` (26), `stories.json` (8), `cases.json` (3), `songs.json` (8), `scenarios.json` (5), `bundles.json` (4), `brands.json` (2), `static-pages.json` (1). `site-config.json` и `page-meta.json` — уже объекты, не тронуты (их в задаче не было). Загрузчик `apps/web/src/lib/site.ts` обновлён — ровно 9 строк-экспортов теперь читают `.items` вместо голого каста, например `export const products: Product[] = (productsData as { items: Product[] }).items;` (было `productsData as Product[]`); типы (`Product`, `BlogPost`, `Landing` и др.) не тронуты. Build чистый (`npm run build -w web`, postbuild check-redirects OK), `npm test -w web` = 29/29 (site.test.ts подтверждает те же инварианты: products=18, blog=26, stories=8, cases=3, songs=8, scenarios=5, bundles=4, brands=2, static-pages=1 + поля целы — обёртка ничего не потеряла), `npm test -w @kk/ui` = 2/2. Сайт функционально идентичен — обёртка только меняет форму JSON на диске и способ чтения в загрузчике. Следующий шаг Фазы 2 — конфиг Sveltia CMS (`/admin`) с file-коллекциями на эти 9 файлов.

## Предыдущая сессия (2026-07-06) — ЛК Фаза 1, Task 6 (финальный): верификация + доки — ФАЗА 1 ЗАВЕРШЕНА

- **ЛК Фаза 1, Task 6 (финальная верификация):** подтверждено «ноль данных» в `site.ts` (`grep -cE "h1: \"|slug: \"|body: \[|title: \"Караоке\"" apps/web/src/lib/site.ts` = 0, `site.ts` = 169 строк чистых типов/загрузчиков), все 11 JSON-файлов на месте (`apps/web/content/{site-config,scenarios,bundles,brands,static-pages,page-meta,products,blog,stories,cases,songs}.json`). Полная сборка+тесты зелёные: `npm run build -w web` чистый (72 статических HTML-файла — 26 статей блога + 18 товаров + лендинги/служебные страницы, postbuild check-redirects OK), `npm test -w web` = 29/29 (5 файлов тестов), `npm test -w @kk/ui` = 2/2. Визуальная сверка через превью (`karaoke-static`, порт 4321, отдаёт свежий `out/`) в обеих темах (светлая и тёмная, переключение через `localStorage['kk-theme']`) — контент идентичен рефактору: главная (hero + квиз-калькулятор), `/catalog` (18 товаров, AST-250 бейдж −44%, цена 1 500 000 ₸ / старая 2 664 000 ₸), `/blog` (26 карточек статей, подтверждено подсчётом DOM-ссылок `a[href^="/blog/"]`), `/product/ast-250` (крошки Главная→Каталог→AST-250, цена 1 500 000, старая цена 2 664 000, блок «Что в комплекте» — 8 позиций из `kit`), `/kontakty` (адрес «Алматы, ул. Муканова, офис 8», телефон «+7 707 579-99-95», почта — все из `site-config.json`). Регрессов не найдено. Обновлена инструкция владельца [`docs/redaktirovanie-sajta.md`](redaktirovanie-sajta.md): все пути `site.ts` → `apps/web/content/*.json` (таблица «Где что лежит», примеры JS→JSON-синтаксиса с кавычками у ключей, правило о запрете висячей запятой в JSON, шпаргалка), добавлена заметка про будущий `/admin` (Фаза 2) со ссылкой на [`docs/client/admin-guide.md`](client/admin-guide.md); `lib/components.ts` (калькулятор) отмечен как не переехавший в `content/` (отдельная логика). **ЛК Фаза 1 (Task 1–6) полностью завершена** — фундамент под CMS Фазы 2 (Sveltia `/admin`) готов: контент в плоских JSON, типизированные загрузчики, тест-сеть безопасности на 29 инвариантов.

## Предыдущая сессия (2026-07-06) — ЛК Фаза 1, Task 5: блог(26)/истории(8)/кейсы(3)/песни(8) → content/*.json

- **ЛК Фаза 1, Task 5:** блог(26)/истории(8)/кейсы(3)/песни(8) вынесены в content/*.json, site.ts читает загрузчиком; 29 тестов зелёные. Весь контент вынесен — осталась финальная верификация (Task 6). Созданы 4 файла: `apps/web/content/blog.json` (26 объектов формы `BlogPost` — `slug`/`title`/`excerpt`/`body: string[]`/`faq: {q,a}[]`), `stories.json` (8 объектов формы `StoryPost`), `cases.json` (3 объекта формы `Case`), `songs.json` (8 объектов формы `Song`). Перенос сделан программно (Node `vm`-парсинг литералов прямо из `site.ts`, без ручной перепечатки) и подтверждён deep-equal против исходных TS-массивов — 0 расхождений. В `site.ts` инлайн-массивы `blogPosts`/`storyPosts`/`cases`/`songsSample` заменены на импорт JSON + типизированные однострочные загрузчики (`export const blogPosts: BlogPost[] = blogData as BlogPost[]` и т.д.). Типы `BlogPost`/`StoryPost`/`Case`/`Song` — не тронуты. После Task 5 в `site.ts` не осталось инлайн-данных вообще (весь контент вынесен Task 2–5) — финальную сверку проведёт Task 6. Build чистый (77 роутов, `/blog/[slug]` генерит все 26 страниц из JSON, postbuild check-redirects OK), тесты `npm test -w web` = 29/29 (integrity: blogPosts=26 уникальные slug + title/body непусты, storyPosts=8, cases=3, songsSample=8), `npm test -w @kk/ui` = 2/2.

## Предыдущая сессия (2026-07-06) — ЛК Фаза 1, Task 4: товары (18) → content/products.json

- **ЛК Фаза 1, Task 4:** товары (18) вынесены в content/products.json, site.ts читает загрузчиком (каст union-типа); 29 тестов зелёные. Создан `apps/web/content/products.json` — verbatim перенос массива `products` (18 объектов: 4 sistema + 6 akustika + 1 sub + 6 mikrofon + 1 miksher), проверен программным deep-equal против исходного TS-массива (совпадение 1:1 по всем полям и порядку). В `site.ts` инлайн-массив `products` (18 объектов, строки 58–110) заменён на импорт JSON + загрузчик `export const products: Product[] = productsData as Product[]` (каст снимает union-narrowing `type: string` → `ProductType`, значения валидирует integrity-тест). Типы `Product`/`ProductType`, `typeLabels`, а также blog/stories/cases/songs — не тронуты (Task 5). Build чистый (77 роутов, `/product/[slug]` генерит все 18 страниц из JSON, postbuild check-redirects OK), тесты `npm test -w web` = 29/29 (включая `products` длина 18, уникальные slug, price>0 конечное число, type в union, inStock boolean), `npm test -w @kk/ui` = 2/2. Задел под Task 5 (blog/stories/cases/songs по тому же паттерну JSON→загрузчик).

## Предыдущая сессия (2026-07-06) — ЛК Фаза 1, Task 3: scenarios/bundles/brands/static-pages + 13 *Meta → content/*.json

- **ЛК Фаза 1, Task 3:** scenarios/bundles/brands/static-pages + 13 *Meta вынесены в content/*.json (page-meta.json), site.ts читает загрузчиком; 29 тестов зелёные. Созданы 5 файлов: `apps/web/content/scenarios.json` (5), `bundles.json` (4), `brands.json` (2), `static-pages.json` (1), `page-meta.json` (объект из 13 ключей — `oNas`/`kontakty`/`sravnenie`/`catalog`/`podKlyuch`/`komplektyIndex`/`kalkulyator`/`pesni`/`keysy`/`blog`/`dlyaDomaV2`/`dlyaBiznesa`/`gotovyeResheniya`, каждый форма `Landing`). В `site.ts` все 4 инлайн-массива и 13 инлайн-`*Meta`-объектов (разбросанных по файлу — часть у строк 110–170, часть в конце у blogPosts/storyPosts) заменены на импорты JSON + типизированные однострочные загрузчики (`export const scenarios: Landing[] = scenariosData`, `export const oNasMeta: Landing = pageMeta.oNas` и т.д.). Типы `Landing`/`Brand`, `mainNav`, `typeLabels`, а также products/blogPosts/storyPosts/cases — не тронуты (Task 4–5). Build чистый (77 роутов, postbuild check-redirects OK), тесты `npm test -w web` = 29/29, `npm test -w @kk/ui` = 2/2 — сеть безопасности `site.test.ts` подтвердила, что данные не потерялись (счётчики + форма Landing всех 13 мета + лендингов/брендов совпали). Задел под Task 4–5 (products/blog/stories/cases по тому же паттерну JSON→загрузчик).

## Предыдущая сессия (2026-07-06) — ЛК Фаза 1, Task 2: siteConfig+songsTotal → content/site-config.json

- **ЛК Фаза 1, Task 2:** siteConfig+songsTotal вынесены в content/site-config.json, site.ts читает загрузчиком; 29 тестов зелёные. Создан `apps/web/content/site-config.json` (verbatim из старого инлайн-объекта `site.ts:1-13` + `songsTotal`). В `site.ts` инлайн-объявление `siteConfig`/`songsTotal` заменено на импорт JSON + типизированный загрузчик (`import siteConfigData from "../../content/site-config.json"`, тип `SiteConfig`, `export const siteConfig: SiteConfig = siteConfigData`, `export const songsTotal = siteConfig.songsTotal`). `resolveJsonModule` уже был включён в корневом `tsconfig.base.json` (который `apps/web/tsconfig.json` наследует через `extends`) — дублировать не потребовалось. Остальной контент в `site.ts` не тронут (Task 3–5). Build чистый (77 роутов, postbuild check-redirects OK), тесты `npm test -w web` = 29/29, `npm test -w @kk/ui` = 2/2 — сеть безопасности `site.test.ts` подтвердила, что данные не потерялись. Задел под Task 3+ (products/brands/blog и т.д. по тому же паттерну JSON→загрузчик).

## Предыдущая сессия (2026-07-06) — ЛК Фаза 1, Task 1: тест-сеть безопасности контента

- **ЛК Фаза 1, Task 1:** тест контент-целостности `site.test.ts` (счётчики+инварианты, web-тестов 24→25 новыми it-блоками; фактически файлов тестов было 4 с 24 тестами включая `CountUp.test.ts`, теперь 5 файлов/29 тестов) — сеть безопасности под вынос контента в `content/*.json`. Файл `apps/web/src/lib/site.test.ts`: 5 `it`-блоков — счётчики коллекций (`products` 18, `scenarios` 5, `bundles` 4, `brands` 2, `staticPages` 1, `blogPosts` 26, `storyPosts` 8, `cases` 3, `songsSample` 8), `siteConfig`-контакты + `songsTotal`, валидность товаров (уникальные slug/цена>0/тип/model/inStock), форма `Landing` у лендингов/брендов/мета-объектов, уникальность и непустота полей блога. Все счётчики совпали с фактом в `site.ts` — правок не потребовалось. Тесты зелёные (`npm test -w web` = 29/29, `npm test -w @kk/ui` = 2/2 без изменений). Задел под Task 2+ (вынос данных в `apps/web/content/*.json`, `site.ts` → тонкий загрузчик).

## Предыдущая сессия (2026-07-06) — Task 6d дизайн-спринта «Сцена»: замыкающий фикс-коммит после финального ревью

- **Контекст:** финальное адверсариальное ревью дизайн-спринта «Сцена» (после Task 6c) нашло 1 MUST-баг сломанного состояния + косметический FAIL + пару SHOULD. Task 6d закрывает всё одним коммитом — спринт окончательно завершён.
- **MUST — сломанный `text-destructive` на `/servis`.** Класс `text-destructive` нигде не был определён в Tailwind-конфигах → CSS-правило не генерировалось → блок «Не покрывается» и иконки `XCircle` красились унаследованным (неконтрастным) цветом вместо красного. Зелёные индикаторы («Покрывается гарантией», иконки `CheckCircle2`, «в наличии» на товаре) были захардкожены дефолтной Tailwind-палитрой (`text-green-600 dark:text-green-400`, `text-green-500`) в обход системы токенов.
  - Фикс: в `packages/tokens/css/theme.css` добавлена пара семантических токенов — `--color-success` и `--color-danger` (light `:root` и `.dark`), посчитан фактический WCAG-контраст (см. ниже), вписан в комментарии.
  - В `apps/web/tailwind.config.ts` и `packages/ui/tailwind.config.ts` — добавлены `success: token("--color-success")`, `danger: token("--color-danger")` (через существующий `color-mix`-хелпер `token()`, чтобы работали opacity-модификаторы вроде `text-danger/60`).
  - Применено: `apps/web/src/app/servis/page.tsx` — `text-destructive`→`text-danger`, `text-destructive/60`→`text-danger/60`, `text-green-600 dark:text-green-400`→`text-success`, `text-green-500`→`text-success`. `apps/web/src/app/product/[slug]/page.tsx` — `text-green-600 dark:text-green-400`→`text-success` (строка рейтинга, скрыта флагом `SHOW_UNVERIFIED_RATINGS=false`), индикатор «в наличии» `text-[#25D366]`→`text-success` (хардкод убран — теперь `#25D366` остаётся ТОЛЬКО за WhatsApp-кнопками).
  - Контраст (посчитан WCAG-скриптом): light — `success #15803d` на `#ffffff` = **5.02:1** ✓ AA, `danger #c02637` на `#ffffff` = **5.88:1** ✓ AA. dark — `success #4ade80` на `#171226` = **10.47:1** ✓ AAA, `danger #ff6b6b` на `#171226` = **6.57:1** ✓ AA+.
  - Grep-аудит полноты (`text-green|text-red|text-destructive|bg-green|bg-red|green-[0-9]|red-[0-9]` в `apps/web/src` + `packages/ui/src`) — после фикса 0 вхождений; других мест не нашлось. Все вхождения `#25D366`/`#1ebe5d` (13 файлов) проверены поштучно — все на WhatsApp-CTA, кроме уже исправленного индикатора наличия.
- **Косметика (FAIL Task 6c) — бейдж скидки 11px.** `apps/web/src/components/DiscountBadge.tsx` держал `text-xs` в базовом className — на `ProductCard.tsx` caller передавал override `text-[11px]`, но в собранном CSS `.text-xs` шёл позже и перебивал (равная специфичность) → бейдж всегда рендерился 12px. Фикс: `text-xs` убран из базового компонента (размер теперь всегда задаёт caller); `ProductCard.tsx` не менялся (уже передавал `text-[11px]`); `product/[slug]/page.tsx` — явный `className="text-xs"` добавлен на вызов, чтобы бейдж на странице товара не остался без размера. Проверено computed `font-size`: карточка каталога = `11px`, страница товара (`ast-250`, скидка 44%) = `12px`.
- **SHOULD — типографика заголовков блога.** `apps/web/src/app/blog/page.tsx` — h1 списка блога был `text-2xl font-medium` без `font-display`, выпадал из иерархии заголовков; теперь `font-display text-2xl font-bold sm:text-3xl`. `apps/web/src/app/blog/[slug]/page.tsx` — h1 статьи был `text-3xl font-semibold tracking-tight` без `font-display`; теперь `font-display text-3xl font-bold tracking-tight`. `HighlightLine` на заголовок статьи намеренно НЕ добавлен (заголовки статей длинные, подсветка легла бы больше чем на половину — по заданию).
- Не тронуто (по заданию): данные `site.ts`, `#25D366`/`#1ebe5d` за WhatsApp-кнопками, `#7c5cff` марка, `CountUp`, механика, SEO-тексты/metadata, флаги `SHOW_UNVERIFIED_*`; should/nice из ревью (стат-гриды, иконки-кружки, pill-капсулы, hero-симметрия, static-page highlight, 404) оставлены в бэклоге на решение владельца.
- Проверено: `npm run build -w web` чисто (статика в `apps/web/out`, `check-redirects` зелёный), `npm test -w web` (24) + `npm test -w @kk/ui` (2) зелёные. Grep: `text-destructive` = 0, `text-green-|text-red-` (дефолтная палитра) = 0 в `apps/web/src` + `packages/ui/src`. Собранный CSS (`apps/web/out/_next/static/css/*.css`) содержит `.text-danger`/`.text-danger\/60`/`.text-success` с корректным `color-mix(...)`. Превью обеих тем (localStorage `kk-theme`): `/servis` — «Покрывается гарантией» зелёным, «Не покрывается» + иконки `XCircle` красным, оба читаемы в light и dark (computed color подтверждён инспектором); `/product/ast-250` — «в наличии» на токене success (не `#25D366`), бейдж скидки `−44%` = 12px (computed); `/catalog` — бейдж скидки на карточке = 11px (computed); `/blog` и `/blog/kak-vybrat-karaoke-dlya-bara` — h1 на `font-family: Unbounded` (`font-display`), `font-weight: 700`. Тёмная тема возвращена (`localStorage.removeItem('kk-theme')`) после проверки.

## Предыдущая сессия (2026-07-06) — Task 6c дизайн-спринта «Сцена»: полировка a11y/perf + доки (ЗАВЕРШЕНИЕ СПРИНТА)

- **Task 6c (полировка + доки) выполнен — это третий и финальный коммит Task 6, дизайн-спринт «Сцена» (Task 1–6) полностью завершён.**
- `apps/web/src/app/product/[slug]/page.tsx` — главное фото товара (above-the-fold) получило `priority` на `<ProductImage>` (LCP: `fetchPriority="high"`, `loading="eager"`); карточки «Похожие товары» на той же странице priority не получили (используют дефолтный lazy).
- `apps/web/src/components/ProductImage.tsx` — новый опциональный проп `decorative?: boolean`: при `true` → `alt=""` (для карточек, где рядом уже есть видимый `<h3>{model}</h3>` в той же ссылке — иначе скринридер читал модель дважды).
- `apps/web/src/components/ProductCard.tsx` — передаёт `decorative` в `ProductImage`; убран мёртвый класс `transition` с корневого `<Link>` (не анимировал ничего после чистки shadow/border-hover в Task 6b); `transition` на `<h3>` и стрелке оставлен (они анимируются по hover).
- `apps/web/src/components/DiscountBadge.tsx` (новый) — вынесена задублированная разметка бейджа скидки (`bg-hot text-hot-fg`, `−{pct}%`) из `ProductCard.tsx` и `product/[slug]/page.tsx`; позиционирование прокидывается через `className` (карточка — `absolute left-2 top-2 text-[11px]`, страница товара — без модификаторов, инлайн рядом с ценой).
- nbsp в тикерах: `apps/web/src/app/page.tsx` (тикер финального CTA) и `apps/web/src/app/dlya-doma/page.tsx` (hero-тикер) — «60 000+» получил неразрывный пробел (U+00A0) между «60» и «000+», чтобы число не переносилось. Остальные вхождения «60 000+» в теле текста (сценарии, FAQ, whatsIncluded) не тронуты — задание касалось только `.ticker`-строк. `dlya-biznesa` тикер содержит только «200+» (без внутреннего пробела разряда) — не требовал правки.
- Мобильные тач-таргеты: главные WhatsApp-CTA (hero + нижний CTA-блок «Написать в WhatsApp» / «Получить расчёт» / «Подобрать в WhatsApp») в `dlya-doma`, `dlya-biznesa`, `pod-klyuch`, `gotovye-resheniya` — `py-2.5` (~40px) → `py-3.5` (≥44px). Вторичные ghost-ссылки «Калькулятор сметы» (`border border-border`) оставлены на `py-2.5` — задание просило поднять только главные CTA.
- `apps/web/src/components/CatalogClient.tsx` — фото-зона `SkeletonCard` (`bg-muted` → `bg-scene`), совпадает с реальной подложкой фото товара. Строки-плейсхолдеры текста остались `bg-muted`.
- `CLAUDE.md` — добавлен подраздел «Анти-AI правила («Сцена», дизайн-директура 2026-07-04)» в «UI-конвенции (обязательные)» (перед «Хлебные крошки»): 9 пунктов (без pill-чипов, CountUp не на витринах, максимум одна grid-секция иконка+заголовок+текст, тени только у floating-элементов, сток-фото запрещён, фото товаров на bg-scene, HighlightLine-приём, один WhatsApp-CTA на вьюпорт, радиусы только из шкалы).
- Блок «🎨 Текущий облик» выше (в шапке этого файла) перезаписан с устаревшей графит/неон-жёлтой схемы (была актуальна до Task 5) на актуальное описание «Сцены».
- Не тронуто (по заданию): данные `site.ts`, `#25D366`/`#1ebe5d`/`#7c5cff`, `CountUp`-компонент, механика квиза/калькулятора, SEO-тексты/metadata, флаги `SHOW_UNVERIFIED_*` (остались `false`).
- Проверено: `npm run build -w web` чисто, `npm test -w web` (24) + `npm test -w @kk/ui` (2) зелёные; превью обеих тем — `/product/ast-250` (фото `fetchpriority="high"` в DOM, бейдж скидки через `DiscountBadge`), `/catalog` (скелетон `bg-scene`, alt карточек пустой), `/dlya-doma` мобайл (CTA ≥44px), главная (тикер «60 000+» с nbsp не переносится).

## Предыдущая сессия (2026-07-06) — Task 6b дизайн-спринта «Сцена»: чистка мёртвого кода

- **Task 6b (чистка) выполнен:** удалены мёртвые `--warm-*`/`--night-*` из `theme.css` и зависимость `lottie-react`, `Button` получил disabled-стили, свод `rounded-2xl`/`rounded-3xl` → `rounded-xl` по всему сайту, тени-«клей» убраны с in-flow карточек (высота сохранена у FAB/оверлеев/sticky), контраст-комментарии выправлены.
- `packages/tokens/css/theme.css` — удалён блок переходных mood-токенов (`--warm-bg/fg/muted/accent/soft`, `--night-bg/fg/muted/accent/soft` в `:root`; переобъявления `--night-bg`/`--night-soft` в `.dark`) — Task 5 убрал все инлайн-использования, grep подтвердил 0 в `apps/web/src`. Заодно поправлены контраст-цифры в комментариях на фактические WCAG-значения (light: fg 17.7, muted-fg 6.92, primary 8.67, cta 5.96, hot 4.79; dark: fg 16.1, muted-fg 7.42, primary 6.42).
- `apps/web/src/app/globals.css` — комментарий про `--color-page` обновлён на актуальные `#f4f2fa` (light) / `#0b0913` (dark) (были устаревшие `#f5f5f5`/`#0e131c`).
- `apps/web/package.json` — убрана неиспользуемая `lottie-react` (LottieHero удалён в Task 3, grep=0 в `apps/web/src`), `npm install` обновил `package-lock.json`.
- `packages/ui/src/Button.tsx` — базовый класс дополнен `disabled:opacity-50 disabled:pointer-events-none` (a11y: `CalculatorClient.tsx` рендерит `<Button disabled>` на шаге без выбора).
- Свод радиусов: `grep -rnE "rounded-2xl|rounded-3xl"` → было 51 вхождение в 16 файлах (`page.tsx`, `dlya-biznesa`, `dlya-doma`, `kontakty`, `brand/[slug]`, `pod-klyuch`, `keysy`, `sravnenie`, `pesni`, `gotovye-resheniya`, `servis`, `o-nas`, `AreaCalculator`, `FaqAccordion`, `CalculatorClient`, `SearchOverlay`, `QuizWidget`) — все заменены на `rounded-xl` (шкала токенов до 12px, банит 2xl/3xl). После — 0.
- Тени: `grep -rnE "shadow-"` дал 5 вхождений. Убрано 2 (in-flow карточки с `border border-border` — тень-«клей»): `servis/page.tsx` (карточка услуги), `AreaCalculator.tsx` (карточка калькулятора площади). Сохранено 3 (функциональная высота): `WhatsAppFAB.tsx` (`shadow-lg`, `fixed` FAB), `ScrollToTop.tsx` (`shadow-md`, `fixed` кнопка), `SearchOverlay.tsx` (`shadow-2xl`, выпадающая оверлей-панель).
- Проверено: `npm run build -w web` чисто (77 роутов), `npm test -w web` (24) + `npm test -w @kk/ui` (2) зелёные; grep-своды `rounded-2xl|rounded-3xl` = 0, `--warm-|--night-` в `apps/web/src` = 0 (кроме доккомментария в `globals.css`), `lottie` в `apps/web/src`/`package.json` = 0; превью light/dark на главной, `/servis`, `/catalog`, `/product/ast-250`, `/kalkulyator` — карточки единый `rounded-xl` без теней-клея, WhatsAppFAB/ScrollToTop сохранили `fixed`+тень+`rounded-full`, disabled-кнопка калькулятора (`opacity: 0.5`, `pointer-events: none`) подтверждена через computed style, палитра light/dark цела.
- Task 6c (полировка + доки, см. выше) выполнен следующим — дизайн-спринт «Сцена» завершён.

## Последняя сессия (2026-07-06) — Task 6a дизайн-спринта «Сцена»: визуальные must-фиксы

- **Task 6a (визуальные must) выполнен:** karaoke-подсветка → слово-заведение (закрыт FAIL Task 5), контраст text-white→text-primary-fg (kontakty/o-nas), блог/новости hairline без теней, keysy на bg-scene, o-nas hero на bg-surface, Header без хардкод-границы, product наличие зелёным, checkout rounded-xl.
- Правки по аудиту (115 находок, workflow): 1) `karaoke/[scenario]/page.tsx` — `highlight = s.h1.split(" ").pop()` вместо эвристики `startsWith("Караоке ")`, которая подсвечивала «для ресторана» (>половины h1); теперь подсвечивается только слово-дифференциатор («дома»/«кафе»/«бара»/«ресторана»/«клуба»). 2) `kontakty` (плашки MapPin/Clock) и `o-nas` (кружок года в таймлайне) — `bg-primary … text-white` → `text-primary-fg` (в тёмной теме primary светлый лавандовый, белая иконка давала ~2.3:1, FAIL AA). 3) `blog/page.tsx` (карточка статьи + карточка истории) и `NewsSection.tsx` — `shadow-sm`/`hover:shadow-md`/`hover:shadow-sm` убраны, `rounded-2xl` → `rounded-xl`, hairline `border-border` + `hover:border-primary` как единственный feedback. 4) `keysy/page.tsx` — карточка кейса `rounded-3xl shadow-sm` → `rounded-xl` без тени; фото-заглушка `bg-gradient-to-br from-surface to-muted` → `bg-scene` (плоская сцена-подложка). 5) `o-nas` hero — инлайн `style={{ background: "var(--color-surface)" }}` → класс `bg-surface`, `rounded-3xl` → `rounded-xl`. 6) `Header.tsx` — убран хардкод `dark:border-white/[0.08]` (токен `border-border` уже флипается через `.dark`). 7) `product/[slug]/page.tsx` — ветка без рейтингов (`SHOW_UNVERIFIED_RATINGS=false`, видимая на проде): «в наличии» → `text-[#25D366]` (WhatsApp-зелёный), «под заказ» → `text-muted-foreground` (было одним цветом `text-accent-fg`). 8) `CheckoutClient.tsx` — три карточки (панель успеха, «корзина пуста», сводка заказа) `rounded-2xl` → `rounded-xl`.
- Не тронуто: данные `site.ts`, WhatsApp-зелёный/марка-фиолетовый, механика квиза/калькулятора, SEO-тексты/metadata, CountUp, `rounded-2xl`/`rounded-3xl` в остальных файлах (лендинги и т.д. — отдельный Task 6b).
- Проверено: build чистый (77 роутов), тесты web 24 + ui 2 зелёные, превью light/dark: `/karaoke/dlya-restorana` — подсвечено только «ресторана» (computed: h1 разбит на две части, вторая — единственное подсвеченное слово); `/kontakty`, `/o-nas` — иконка/цифра на `bg-primary` = `text-primary-fg` (dark: тёмный `#170f38` на лавандовом; light: белый на фиолетовом — оба AA); `/blog` — карточки без `box-shadow`, `rounded-xl`; `/keysy` — заглушка `bg-scene` (не градиент); `/product/ast-250` — «в наличии» = `rgb(37,211,102)` (`#25D366`) в обеих темах; `/checkout` — все три карточки `rounded-xl`, без тени.
- **Следующий шаг: Task 6b** (по HANDOFF-плану Task 5) — либо продолжение визуальных хвостов из аудита, либо удаление неиспользуемых `--warm-*`/`--night-*` переменных из `theme.css`.

## Предыдущая сессия (2026-07-05) — Task 5 дизайн-спринта «Сцена»: лендинги

- **Task 5 (лендинги) выполнен:** все mood-инлайны (--warm-*/--night-*) и хардкод-градиенты мигрированы на токены, сток scenariy/ удалён (3 jpg) → bg-scene плейсхолдеры, h1 лендингов с HighlightLine, чипы → тикеры, CountUp убран с dlya-doma/dlya-biznesa.
- Файлы: `dlya-doma`, `dlya-biznesa`, `pod-klyuch`, `gotovye-resheniya` (SolutionCard warm/night-ветвление убрано — один токен-вариант), `sravnenie`, `brand/[slug]` (accentVar/night-ветвление убрано, подсветка имени бренда в h1) — все на `border-border bg-background`/`bg-surface`/`text-muted-foreground`/`text-primary`.
- `LandingPage.tsx` (шаблон для `komplekty` индекс, `karaoke/[scenario]`, `(pages)/[page]`) — подсветка через явный проп `highlight` (эвристика splitH1 заменена после ревью): страница передаёт осмысленную подстроку h1; не передана/не найдена → h1 целиком без подсветки. Call-sites: komplekty «по площади», karaoke/[scenario] — хвост «для кафе» и т.п., (pages)/[page] — первое слово («Сервис»). `komplekty/[area]` (свой markup) — HighlightLine на части с площадью («до 30 м²»).
- `apps/web/public/scenariy/` удалён полностью (poyushchie.jpg осиротел ещё в Task 3; dom.jpg/biznes.jpg заменены на bg-scene плашки «ДЛЯ ДОМА»/«ДЛЯ БИЗНЕСА»).
- Проверено: `grep scenariy` = 0, `grep -- "--warm-\|--night-"` = 0 (кроме одной документационной строки-комментария в `globals.css`, не usage), build 77 роутов чисто, тесты web 24 + ui 2 зелёные, превью light/dark на 7 страницах + мобильный вьюпорт (dlya-doma, pod-klyuch) — читается, WhatsApp-кнопки зелёные, нет форс-тёмных hero в светлой теме.
- **Task 6 (следующий):** удалить сами `--warm-*`/`--night-*` переменные из `theme.css`, блог/служебные страницы.

## Что это
Полный ребилд интернет-магазина караоке-оборудования (Алматы; бренды AST + Studio Evolution + проф-оборудование). Цели: B2C-продажи + B2B-лиды (оснащение под ключ) + бренд + **SEO-приоритет**.

## Где живёт (важно)
- 🟢 **Live (боевой прод):** https://karaokeshop.airg-inggger.workers.dev/ — = HEAD `main`, проверять надо **именно его, не `.kz`**.
- ⚠️ **Бренд-домен `karaokeshop.kz` ещё НЕ привязан — отдаёт СТАРЫЙ сайт на Wix** (DNS не переведён на Cloudflare). При этом `siteConfig.url = "https://www.karaokeshop.kz"` → `canonical`/`og:url`/`og:image` нового сайта ведут на чужой Wix = **SEO-риск до привязки домена**. Долг известный, `siteConfig.url` пока **не трогаем** — закроется в момент привязки домена. Чек-лист «Проверка после деплоя» + детали — [deploy.md](deploy.md).
- **Repo:** github.com/airginggger-collab/Karaokeshop · ветка `main`
- **Локально:** `~/Desktop/karaokeshop` — **ОТДЕЛЬНЫЙ проект, не medlog** (сессии часто стартуют из каталога medlog).
- **Коммит/пуш:** только `git -C ~/Desktop/karaokeshop`. Bash сбрасывает cwd на medlog между вызовами. Push по **SSH** (ключ `~/.ssh/id_ed25519` работает). Identity репо: `airginggger-collab <airg.inggger@gmail.com>` (в local git config). `gh` CLI не авторизован (токен в Keychain, фону недоступен).
- **Деплой:** Cloudflare assets-only Worker (`wrangler.toml` → `apps/web/out`), **авто на каждый push в main**. Превью-MCP привязан к medlog — проверять надо `build` + `curl` живого URL.

## Стек
Next.js 15 (App Router, `output: "export"` — статика) · Turborepo (npm workspaces) · Tailwind на дизайн-токенах (Style Dictionary) · Storybook · lucide-react · шрифты Manrope + Unbounded. **77 статических страниц** (26 блог-статей + 18 товаров + служебные). Тесты Vitest: web 24 (calculator 12 + seo 3 + quiz 4 + CountUp 5) + ui 2. `lottie-react` удалён (Task 6b, 2026-07-06) — LottieHero был убран ещё в Task 3, зависимость висела мёртвой.

## Сборка / проверка
```
npm run build -w web      # сборка (→ apps/web/out)
npm test -w web           # тесты
```
После пуша — `curl` живого URL (превью-MCP не для этого проекта).

## Где контент/данные (для правок)
- `apps/web/src/lib/site.ts` — товары (18 шт), бренды, блог, кейсы, песни, `siteConfig` (телефон/адрес), мета-страниц.
- `apps/web/src/lib/components.ts` — цены оборудования в калькуляторе сметы.
- `apps/web/public/products/` — фото товаров (поле `image` у товара).
- Инструкция для владельца (новичок, через браузер): [docs/redaktirovanie-sajta.md](redaktirovanie-sajta.md) (+ .docx).

## Сессия 2026-07-05 — фикс: opacity-модификаторы Tailwind не генерировали CSS

- **Баг:** цвета в `tailwind.config.ts` были заданы простыми строками `"var(--color-*)"` — Tailwind 3 не умеет вычислить из такой строки прозрачность (`parseColor` → null), и классы вида `text-primary/40`, `bg-foreground/40`, `bg-primary/5`, `focus:ring-primary/30` **молча выпадали из сборки** (в `out/…css` не было ни одного правила). Затронуты были: главная (номера секций, плашка «AST · EVOBOX»), pod-klyuch, keysy, sravnenie (зебра `bg-surface/40`), blog, servis, плейсхолдер ProductImage, подложка SearchOverlay (`bg-foreground/40` — оверлей был вовсе без затемнения), CalculatorClient, focus-ring в CheckoutClient.
- **Фикс:** в `apps/web/tailwind.config.ts` и `packages/ui/tailwind.config.ts` цвета отдаются как `color-mix(in srgb, var(--color-*) calc(<alpha-value> * 100%), transparent)` (хелпер `token()`). Токены `theme.css` **не тронуты** — hex остаётся единственным источником, прямые потребители `var(--color-*)` (globals.css, инлайн-стили brand/o-nas) не затронуты; RGB-триплеты не заводили (дублирование палитры ×2 темы = риск рассинхрона). Без модификатора получается `calc(1 * 100%)` — исходный цвет, регрессий нет (проверено computed-стилями: `text-primary` = тот же hex с альфой 1).
- Проверка: build чистый, тесты web 24 + ui 2 зелёные, превью обеих тем: `text-primary/40` = `#9d8bf0`/40% (dark) и `#4c30b8`/40% (light).

## Сессия 2026-07-04 — полный аудит + спек дизайн-спринта «Сцена»

- **Аудит** (архитектура / UX-конверсия / продуктовая готовность, 3 параллельных отчёта): технически проект здоров (тесты, CI, прод), но показ живым людям блокируют: фейковое соцдоказательство (отзывы, «логотипы» клиентов, кейсы, рейтинги товаров — выдуманы), лид-пайплайн только WhatsApp без сохранения заявок, аналитика нулевая (Метрика — заглушка, GA4/GSC нет), юридики нет (политика/оферта/реквизиты). Дорожная карта: этап «друзья» (вычистить фейк, checkout, аналитика, юридика) → «прод» (домен, GSC, фото, лид-endpoint) → «инвестор» (метрики, CRM, модель роста).
- **Приоритет владельца:** убрать «иишный» дизайн. Брейнсторм-решения: B2C-дом первичен; якорей бренда нет (можно менять всё); объём — весь сайт; из трёх директур (Сцена / Ретро-глэм / Плакат) выбрана **A «Сцена»**; секции спека утверждены по одной. Визуальные варианты показывали inline-виджетами (mcp__visualize), как в прошлый раз.
- **Спек записан:** [specs/2026-07-04-dizayn-sprint-stsena-design.md](superpowers/specs/2026-07-04-dizayn-sprint-stsena-design.md) — фиолетовая палитра от лого (CTA `#7c5cff`, page `#0b0913`, hot `#ff5c8a`), подсветка строки как фирменный приём, анти-AI правила, покрытие всех страниц, 6 шагов реализации (токены → шелл → главная → каталог/товар → лендинги → блог/служебные).
- **План реализации записан:** [plans/2026-07-04-dizayn-sprint-stsena.md](superpowers/plans/2026-07-04-dizayn-sprint-stsena.md) — 6 задач (токены → шелл/HighlightLine → главная → каталог/товар → лендинги → финал+правила), каждая = зелёный коммит с build+тесты+превью обеих тем+CI. Реализация ещё не начата.
- **Task 1 (токены) выполнен:** палитра «Сцена» в theme.css (light/dark), mood-блоки удалены, warm/night переходно на фиолете, радиусы 10/12, tailwind hot/scene, globals без теней-клея + классы .hl/.ticker. ⚠️ Контраст-чек: белый на cta тёмной `#7c5cff` (марка лого) фактически **4.35:1** (< AA 4.5) — остальные 5 пар PASS. Контраст-фикс: cta dark **`#7250f8`** (белый текст 4.98:1 ✓ AA); марка лого в Header остаётся `#7c5cff`; спек и план обновлены. Пост-ревью фикс: глобальный `border` с `.bg-background` убран (shorthand перебивал border-утилиты — рамка Hero `border-2 border-primary`, зебра sravnenie, непрошеная рамка шапки/моб.меню); hairline `border border-border` задан явно на карточках: главная (шаги, сценарные входы, товары), Hero-бейдж, QuizWidget, ServiceSteps, CheckoutClient ×3.
- **Task 2 (шелл) выполнен:** HighlightLine (SSR-safe подсветка строки), активные пункты Header/MobileNav через .hl, тикер в Footer вместо бренд-пилюль, Button без теней (primary brightness-ховеры, ghost прозрачный). ⚠️ Пост-ревью фикс: в MobileNav `.hl`'s `padding` (shorthand) перебивал `px-4 py-3.5` активного пункта и схлопывал тач-зону (56px→~1px) — добавлены `!px-4 !py-3.5` поверх `.hl` на активной ссылке, тач-зона подтверждена превью (56px, padding 14px/16px). Спек-ревью фикс: базовый `rounded-xl` перебивал `rounded-[4px]` активного пункта (равная специфичность, порядок в скомпилированном CSS) — `rounded-xl` вынесен в неактивную ветку (без !important); превью: активный 4px, неактивные 12px, тач-зоны 56px без изменений. Рефактор по ревью качества: `.hl`/`.ticker` обёрнуты в `@layer components` (слой ДО utilities — утилиты на элементе побеждают без !important; `!px-4 !py-3.5`/`rounded-[4px]` из nav убраны), `border-radius: 4px` теперь часть `.hl`; HighlightLine — FOUC-гард (первый IO-колбэк только измеряет: уже видимый при гидрации элемент остаётся залитым без анимации); `aria-current="page"` на активных пунктах Header/MobileNav. Computed-проверка обеих тем: активные 4px/заливка cta/белый текст, drawer-пункт 56px (padding 14px/16px), тикер на месте (не выпал из tree-shake).
- **Task 3 (главная) выполнен:** hero «Сцена» (тикер+h1 с подсветкой+квиз 7/12+сцена-плашка), секции 01–04 с нумерацией, CountUp/чипы/сток убраны с главной, отзывы+ClientLogos скрыты флагом SHOW_UNVERIFIED_SOCIAL_PROOF, финальный CTA с тикером цифр, Hero.tsx удалён (мёртвый код). Также удалены LottieHero.tsx и HeroWave.tsx (0 импортов после правки). Мелочь из ревью: HighlightLine — армированная ветка IO срабатывает от `intersectionRatio >= 0.35` (раньше `isIntersecting` стартовал анимацию при ~1px видимости). QuizWidget: механика не тронута, обёртка rounded-xl, прогресс-бары через `bg-cta`/`bg-border` вместо инлайн-стилей. SEO: h1 сменился на «Караоке-вечер у тебя дома», метадата не тронута. Превью-проверка обеих тем + мобайл (квиз-кнопки 46px), квиз прокликан 3 шага (результат AST 250 + WhatsApp/смета). Хвосты ревью (follow-up-коммит): удалён осиротевший `AnimationToggle` (единственный контрагент событий `kk-anim-*` был HeroWave; убран из layout.tsx, grep `kk-anim`/`anim-paused` по коду = 0); HighlightLine — IO-колбэк берёт последнюю запись батча (`entries[entries.length - 1]`, была первая = самая старая); `aria-hidden="true"` на декоративных номерах секций (SectionTitle) и плашке «AST · EVOBOX».
- **Task 4 (каталог+товар) выполнен:** hairline-карточки, bg-scene подложка фото (белые плашки убраны), табы фильтров через .hl, иерархия CTA (WhatsApp→корзина→ghost), фейковый рейтинг скрыт флагом SHOW_UNVERIFIED_RATINGS и убран из JSON-LD. ⚠️ Обнаружен (не мой баг, пред-существующий с Task 3): классы-модификаторы прозрачности вида `text-primary/40` по всему сайту (page.tsx, blog, sravnenie, servis, теперь и ProductImage-плейсхолдер) не генерируют CSS вообще — `tailwind.config.ts` задаёт цвета как `"var(--color-primary)"` (не RGB-triplet формат), поэтому Tailwind не может применить альфа-модификатор; в рантайме класс просто не работает (фолбэк на унаследованный `--color-fg`, визуально не критично — foreground достаточно контрастен, но эффект «приглушённости» отсутствует). Заведена фоновая задача на отдельный фикс, в Task 4 не чинил (вне скоупа, затрагивает весь сайт). Микро-фиксы по ревью (follow-up-коммит): токен `--color-hot-fg` (light `#ffffff` — 4.79:1 на hot `#d6296b` ✓ AA; dark `#2a0a14` — 6.21:1 на hot `#ff5c8a` ✓ AA+, белый давал 2.94:1 FAIL) + tailwind `hot-fg`, бейджи скидки в ProductCard/странице товара `text-white` → `text-hot-fg`; a11y CatalogClient: `aria-pressed={active}` на кнопках-фильтрах, `aria-label="Фильтр по типу"` на контейнере табов. Замечания ревью 3–10 (opacity-40 обход, LCP priority, DiscountBadge, alt, скелетон, зелёный inStock, lib/flags) — распределены на Task 6, здесь не трогались.

## Сессия 2026-07-02 (5) — новая цветовая схема: графит (свет) / неон-жёлтый (тёмная)

Ветка `light-graphite-dark-yellow`. Только `theme.css` + `globals.css` (+ hero дом).
- **Светлая тема:** акцент **графит `#15181e`** (сине-чёрный, в тон тёмной `#0e131c`) — primary/accent/cta, fg белый. Фон страницы `--color-page` `#f5f5f5` → **`#e3e7ee`** (прохладный) + карточки **белые** (`.bg-background` градиент → `#fff→#f7f8fa`, тень усилена) — раньше карточки сливались с фоном.
- **Тёмная тема:** акцент **неон-жёлтый `#facc15`** (primary/accent/cta + `--night-accent`), fg тёмный `#2a2400`.
- WhatsApp-зелёный не тронут. Оба варианта AAA по контрасту. hero «Дом» — градиент под новые белые карточки.
- 📌 Подбор оттенков делали через inline-виджеты (mcp__visualize) — компаньон-браузер у заказчика не открывался.

## Сессия 2026-07-02 (4) — тёмная тема по умолчанию + доки для заказчика

- **Тёмная тема — основная:** класс `dark` на `<html>` из SSR ([layout.tsx](../apps/web/src/app/layout.tsx)); скрипт снимает его только при явном выборе светлой (`kk-theme==='light'`). Дефолт (без сохранённой темы) = тёмная, без вспышки. Переключатель работает.
- **Доки для заказчика:** [client/chto-nuzhno-ot-vas.md](client/chto-nuzhno-ot-vas.md) — простым языком, что нужно от владельца и зачем (домен, старый сайт, GSC, Metrica-ID, GBP/2ГИС, фото) + рекомендация остаться на `karaokeshop.kz`. Технический план — `docs/strategy/domain-launch.md`.

## Сессия 2026-07-02 (3) — постмортем: `_redirects` уронил деплой

**Причина:** абсолютный URL в `apps/web/public/_redirects` (www-канонизация) → Cloudflare отклоняет на `wrangler deploy` («Only relative URLs are allowed») → **деплой падал, прод молча застрял** на терракоте (синий апдейт не выкатился). Локальный `next build` файл не валидирует — баг всплыл только в CI-деплое, а CI после пуша я не проверял.
**Фикс:** убрал абсолютное правило (относительные only); www-канон → Cloudflare Redirect Rules (дашборд). **Защита:** `postbuild`-хук [`scripts/check-redirects.mjs`](../scripts/check-redirects.mjs) роняет локальную сборку на абсолютных URL. Ловушка №0 в [/CLAUDE.md](../CLAUDE.md): после пуша в `main` **всегда `gh run list`** — деплой мог упасть.

## Сессия 2026-07-02 (2) — светлая тема: сине-стальной акцент + тёмный финальный блок

Ветка `light-blue-accent`.
- **Светлая тема:** акцент → **`#1e40af`** (сине-стальной): `:root` primary/accent/cta = `#1e40af`, fg белый, soft = синий тинт `#e3e9f7`. Заменяет прежний янтарь/золото в светлой — links, кнопки, лейблы, мелкие «жёлтые» кружки/панели (`bg-primary-soft`) теперь синие. [theme.css](../packages/tokens/css/theme.css).
- **Тёмная тема — НЕ тронута** (терракота `#d9682e`). 📌 **Будущий вариант:** тёмная → `#60a5fa` (ближе к бренд-навы) — по решению владельца «на потом».
- **Финальный блок «Подберём за минуту»** ([page.tsx](../apps/web/src/app/page.tsx)) — из жёлтого (`bg-primary-soft`) в **тёмную секцию** (градиент `#1e2b40→#131b29`, белый текст, WhatsApp зелёный, ghost-кнопка на белой обводке).
- **Карточки `/kontakty`** («Подъехать»/«Выезд») — с плоского тинта `bg-primary/5` на градиент-стиль `bg-background` (как «Как с нами связаться»).
- **Не тронут:** `--warm-accent` (коричнево-оранжевый на лейбле «ДЛЯ ДОМА», ценах «дом») — общий токен обеих тем; менять = задеть тёмную. Кандидат на отдельную правку, если нужен синий и там.

## Сессия 2026-07-02 — оранж в тёмном, счётчики, фоны hero, без верх. обводки

Итерация после янтарного апдейта (ветка `redesign-amber-2`). Спек: [specs/2026-07-02-vizual-2-oranzh-schetchiki-design.md](superpowers/specs/2026-07-02-vizual-2-oranzh-schetchiki-design.md).

- **Акцент менее жёлтый в ТЁМНОМ:** `.dark` primary/accent/cta + `:root --night-accent` `#f59e0b` → **`#d9682e`** (терракота — гармонирует с тёплым серым). Светлая тема (глубокий янтарь текст `#b45309` + золотые кнопки `#f59e0b`) — НЕ тронута. [theme.css](../packages/tokens/css/theme.css).
- **Hero «Для бизнеса»** — фон не чёрный: `var(--night-bg)` → градиент `#1e2b40 → #131b29`.
- **Hero «Для дома»** — тёплый серый: `from-surface to-muted` → `from-white to-[#eeeeec]`.
- **Анимация счётчиков:** новый [CountUp.tsx](../apps/web/src/components/CountUp.tsx) — числа растут от 0 при появлении в вьюпорте (IntersectionObserver + rAF, ease-out ~1.1с), уважает `prefers-reduced-motion`, SSR-safe (финальное значение до анимации). Применён к trust-блоку главной + «200+» в hero бизнеса. Парсер `parseValue` покрыт тестами (+5). Тесты web 19→**24**. ⚠️ vitest include = `src/**/*.test.ts` (только `.ts`, не `.tsx`) — тесты класть в `.ts`.
- **Убрана верхняя 3px-обводка** во всех hero/карточках: dlya-doma, dlya-biznesa (×2), pod-klyuch, brand/[slug], o-nas.

## Сессия 2026-07-01 (2) — янтарный ре-колор + фото

Визуальный апдейт по референсу (тёмный + янтарь). Ветка `redesign-amber`.

- **Цвет:** фирменный акцент тил/зелёный → **янтарь** (только [theme.css](../packages/tokens/css/theme.css)). Тёмная: `#f59e0b`. Светлая: текст/ссылки/цены — глубокий `#b45309` (AA на белом).
- **Золотые кнопки (светлая тема):** новый токен `--color-cta` (`#f59e0b` + тёмный текст) для primary-кнопок — чтобы кнопки «светились» золотом, а ссылки оставались читаемыми. Переведены: `@kk/ui` Button (primary), инлайн-пилюли в CalculatorClient/CatalogClient/BrandProductFilter. Ссылки/бордеры/тинты — на `--color-primary`. Маппинг `cta`/`cta-fg` — в [tailwind.config.ts](../apps/web/tailwind.config.ts).
- **WhatsApp-зелёный** (`#25D366`) — не тронут.
- **Фото** (Unsplash, jpg — sips не дал webp): [apps/web/public/scenariy/](../apps/web/public/scenariy/) `poyushchie.jpg` (поющие, в hero главной справа), `dom.jpg`/`biznes.jpg` (секция «Дом и бизнес» на главной после «Как работаем» + hero-фото на `/dlya-doma` и `/dlya-biznesa`). Изначальные кандидаты поющих оказались Unsplash+ премиум → взято свободное фото.
- **Светлая тема — тинт карточек:** мятный градиент `.bg-background` ([globals.css](../apps/web/src/app/globals.css)) заменён на тёплый светлый серый `#eeeeec` — по запросу; тёмная версия градиента (синяя) не тронута.
- **Интерактивные состояния:** primary-кнопка ([Button](../packages/ui/src/Button.tsx)) меняет цвет на hover (`brightness-90`) и клик (`brightness-.82` + scale); ghost — hover-бордер/фон, active-фон. Trust-цифры на главной — hover-лифт (border-primary + shadow + scale цифры).
- Проверено в **обеих темах** (desktop+mobile): акцент янтарный, кнопки золотые/читаемые, фото грузятся, тесты web 19 / ui 2 зелёные.
- Спек: [specs/2026-07-01-vizualnyy-apdeyt-amber-design.md](superpowers/specs/2026-07-01-vizualnyy-apdeyt-amber-design.md) · План: [plans/2026-07-01-vizualnyy-apdeyt-amber.md](superpowers/plans/2026-07-01-vizualnyy-apdeyt-amber.md).

## Сессия 2026-07-01 — главная пересобрана под «подбор-как-услугу»

Брейнсторм → спек → план → реализация (ветка `redesign-home-usluga`, мердж в `main`). Главная теперь продаёт экспертный подбор, а не «коробку».

- **Hero = квиз-подбор** (A1): тонкий слоган «Караоке без ошибки в выборе» + чипы доверия + `QuizWidget` в первом экране. Старый bento-hero (`LottieHero`/`HeroWave`), отдельная квиз-секция и блок «С чего начать» удалены.
- **`QuizWidget` → инлайн-результат:** после 3 вопросов показывает карточку (рекомендованная база + ориентир цены) на базе `configureWithinBudget`, затем «Заявка в WhatsApp» / «Открыть полную смету» ([QuizWidget.tsx](../apps/web/src/components/QuizWidget.tsx)).
- **`lib/quiz.ts`** (новый) — маппинг ответов квиза → вход калькулятора (площадь/бюджет/venue), покрыт тестами (+4). Тесты web 15→**19**.
- **Порядок секций:** hero-квиз → Как мы работаем → доверие (дилер/цифры/лого/отзывы/почему) → «На чём собираем» (оборудование как доказательство) → Разделы → финальный CTA.
- Спек: [specs/2026-07-01-glavnaya-podbor-usluga-design.md](superpowers/specs/2026-07-01-glavnaya-podbor-usluga-design.md) · План: [plans/2026-07-01-glavnaya-podbor-usluga.md](superpowers/plans/2026-07-01-glavnaya-podbor-usluga.md).
- **Фаза 2 (не сделано):** отдельная услуговая страница (Вариант C — сервис-лендинг). Отдельным спеком.

## Сессия 2026-06-25 — QA-прогон LIVE + фиксы находок

Полный QA-прогон сайта (head-of-QA). Подтверждено: XSS нет (React экранирует), все 70 URL → 200, корзина/checkout/визард/поиск/тема/моб-меню работают. Найдено и **исправлено** (build+tests зелёные, проверено на свежем `out/`):

1. **Калькулятор — бюджет был плацебо.** `buildSmeta` звал `configure()` и игнорировал `budgetIdx` → смета одинакова при любом бюджете (и превышала лимит). Добавлен `configureWithinBudget(input, budget)` в [calculator.ts](../apps/web/src/lib/calculator.ts) — триммит опции (свет → сабвуфер → микрофоны до 2) под выбранный бюджет, **сохраняя площадь** (в отличие от `configureByBudget`). Подключён в [CalculatorClient.tsx](../apps/web/src/components/CalculatorClient.tsx) + заметка о тримминге/превышении. +5 тестов. Вживую: «до 1М» теперь 978 000 ₸ (было 1 328 000 ₸).
2. **Хлебные крошки** добавлены на 4 динамических роута (нарушали правило CLAUDE.md): `blog/[slug]`, `komplekty` (индекс), `komplekty/[area]`, `karaoke/[scenario]`. В [LandingPage.tsx](../apps/web/src/components/LandingPage.tsx) добавлен опциональный проп `breadcrumb`.
3. **`/og.jpg` не существовал** (404 на всём сайте → шеринг без превью). Создан брендированный 1200×630 [apps/web/public/og.jpg](../apps/web/public/og.jpg) (SVG→JPG через `sips`).
4. **Яндекс.Метрика с заглушкой `XXXXXXXX`** грузила битый пиксель на всех страницах. В [layout.tsx](../apps/web/src/app/layout.tsx) гард `YM_ENABLED` (regex на числовой ID) — пока ID заглушка, Метрика не рендерится. Вставить реальный числовой ID, чтобы включить.
5. **Корзина** теперь санитайзит `localStorage` при загрузке ([cart.tsx](../apps/web/src/lib/cart.tsx) `sanitizeCart`): qty→целое ≥1, цена — конечное число ≥0, дедуп id, отброс мусора.

**Доп. проход по гайду «Основы дизайна» (§7 формы, §9 тач-зоны):** в [CheckoutClient.tsx](../apps/web/src/components/CheckoutClient.tsx) поля «Имя»/«Телефон» получили видимые лейблы над полями (был placeholder вместо лейбла), placeholder теперь пример/маска (`Напр., Айгерим`, `+7 (___) ___-__-__`), + `autoComplete`/`inputMode`. Кнопка «Убрать» в заказе — тач-зона 44×44 (была ~14px). Остальное по гайду уже соблюдено (шкала отступов Tailwind 4px, цвета через токены, фон страницы off-white, `text-white` только на цветных кнопках) — токены не трогали.

**Осталось (не код — деплой/DNS) ⚠️ SEO-риск:** canonical/`og:image`/`og:url` абсолютны на `https://www.karaokeshop.kz`, но домен ещё **НЕ привязан** — `karaokeshop.kz` сейчас отдаёт **старый сайт на Wix** (подтверждено аудитом деплоя 2026-06-26). Значит canonical и соц-превью нового сайта ведут на чужой Wix, пока домен не переведён на Cloudflare. Боевой прод — только `*.workers.dev` (= HEAD, всё ок); **проверять надо его, не `.kz`**. Социальные превью и canonical станут корректны автоматически после привязки домена; `siteConfig.url` до этого не трогаем. og-ассет уже на месте. Лог-артефакт: локальный `next build` иногда отдаёт устаревший HTML из кэша даже после `rm -rf .next out` — структуру проверять `curl` живого URL. Чек-лист «Проверка после деплоя» — [deploy.md](deploy.md).

## Сессия 2026-06-24 (продолжение 8) — permissions allowlist, меньше апрувов

`.claude/settings.json` создан (gitignored, локальный). 24 паттерна без апрува:
- Все `mcp__Claude_Preview__preview_*` — скриншоты, клики, eval, логи, старт/стоп
- `mcp__visualize__show_widget` + `read_me`
- `Bash(npm run build -w web)`, `Bash(npm test -w web)`, `Bash(npm test -w @kk/ui)`, `Bash(npm run tokens)`
- `Bash(git -C ~/Desktop/karaokeshop push origin main)`
- `Bash(npx tsc --noEmit *)`, `Bash(gh run list *)`

Апрув остаётся на: `git add`, `git commit`, `rm -rf`, `sed -i` — мутирующие операции.

## Сессия 2026-06-24 (продолжение 7) — оптимизация производительности

- **Изображения товаров сконвертированы в WebP** (sharp): `ast-250.jpg` 57KB→5KB, `ast-250-3.png` 180KB→30KB, `ast-350-2.png` 32KB→7KB, `ast-250-2.png` 113KB→16KB. Ссылки в `site.ts` обновлены.
- **Lottie — динамический импорт** (`next/dynamic`, `ssr: false`) — библиотека lottie-react больше не блокирует первый рендер, грузится после hydration.
- **ProductImage** — добавлен пропс `priority`: `fetchPriority="high"` + `loading="eager"` для above-the-fold картинок; `decoding="async"` на всех.

## Сессия 2026-06-24 (продолжение 6) — «Как я работаю» для портфолио и клиентов

Созданы два файла:
- `docs/kak-ya-rabotayu.md` — методология в Markdown (для проекта, GitHub)
- `docs/kak-ya-rabotayu.html` — standalone страница с тёмным дизайном (открывается в браузере, для портфолио / PDF-экспорта клиентам)

6 этапов: интервью → брейншторм → утверждение визуала → сборка → проверка → передача. Каждый этап — ловушки из реального опыта karaokeshop.

## Сессия 2026-06-24 (продолжение 5) — секция «Инструменты» в портфолио

Добавлена секция «Инструменты и расширения» в `docs/portfolio-case-study.md`: Claude Code CLI, MCP-серверы (Preview, gh), встроенные инструменты, что не использовалось.

## Сессия 2026-06-24 (продолжение 4) — fix Lighthouse не красит CI в красный

**Проблема:** `lighthouserc.json` — `categories:seo` был на уровне `error`, при падении SEO-балла ниже 0.95 весь CI-ранн показывался красным, хотя деплой проходил (Lighthouse-джоб независимый). Это и были "ошибки при заливе" — деплой шёл, но GitHub показывал ❌.

**Исправление:** `categories:seo` переведён с `["error", ...]` на `["warn", ...]`. Теперь все ассерты Lighthouse — только `warn`. CI ❌ → только если реально сломалась сборка или тесты.

## Сессия 2026-06-24 (продолжение 3) — усиленный градиент карточек

- **Градиент на всех карточках** (`globals.css`, `.bg-background`) усилен: light `#fff→#ddf5f1`, dark `#1f3558→#0f1928`. Предыдущие стопы `#f0faf9`/`#2d4068→#1e2d48` были слишком слабые — теперь градиент виден отчётливо.

## Сессия 2026-06-24 (продолжение 2) — градиент карточек + стратегии

- **Градиент на всех карточках** (`globals.css`, `.bg-background`): light `#fff→#f0faf9`, dark `#2d4068→#1e2d48`. Один файл — все карточки сайта разом. Техника: `background-image` поверх Tailwind `background-color` — не конфликтуют.
- **«14+ лет»** вместо «с 2012» в UI (герой, trust, дилерская полоса, /o-nas).
- **Стратегия роста** `docs/strategy/growth-strategy-2026.md` — LinkedIn, партнёры, выход из сарафана, план 90 дней.
- **Конкурентный анализ** `docs/strategy/competitor-analysis-2026.md` — 10 игроков, стратегия обхода по 5 направлениям.

## Сессия 2026-06-24 (продолжение) — стратегия роста

`docs/strategy/growth-strategy-2026.md` — стратегия выхода из сарафана: LinkedIn (личный бренд + аутрич + контент-план), партнёрский канал (дизайнеры/ремонтники/event), холодный WA-аутрич по базе заведений, реферальная программа. Приоритетный план 90 дней с бюджетом ~30 000 ₸.

## Сессия 2026-06-24 — герой, анимация, гео-сигналы, конкурентный анализ

- **HeroWave: `mix-blend-mode: screen`** — орбы теперь горят как неон (было opacity на тёмном = тухло). 4 орба: teal (600px), оранжевый (460px), teal-мягкий (320px), фиолетовый (300px). Добавлена двухостановочная градиентная сердцевина у крупных орбов.
- **«14+ лет»** — заменено везде в UI: герой (stats), trust-блок, дилерская полоса, `/o-nas`. Текстовые контент-описания оставлены с «с 2012» (SEO).
- **Гео-сигналы Алматы** — badge «📍 Шоурум · Выезд по Алматы за 1 час» в дилерской полосе (ссылка на /kontakty). На /kontakty — два новых блока: «Подъехать в шоурум» и «Выезд на объект · за 1 час · замер бесплатно».
- **Анализ конкурентов** — `docs/strategy/competitor-analysis-2026.md`: 10 конкурентов, стратегия по 5 направлениям, план на 3 месяца.
- **Портфолио** — `docs/portfolio-case-study.md`: кейс для портфолио от нуля до прода.

## Итоги сессии 2026-06-23 (вечер) — сводка всех изменений

### Контент и SEO
- **SEO-блог пакет 3** (+8 статей, итого 26): гостиница/банкет, корпоратив, подключение к TV, гарантия/ремонт в РК, световое оборудование, обновление песен, Астана+Шымкент, расчёт бюджета для заведения
- **Лента историй** (`storyPosts` в `site.ts`, 8 карточек) — секция на `/blog`, masonry, аватар-инициалы, теги, лайки
- **Авто-новости** (`src/lib/news.ts` + `NewsSection.tsx`) — build-time fetch Google News RSS, fallback 6 статей, секция в конце `/blog`

### UI / компоненты
- **ProductCard** — убрана зелёная WhatsApp-кнопка «Узнать цену»; заменена на «Подробнее →» рядом с ценой. Компонент стал серверным (убран `"use client"`)
- **Карточки главной** — удалены декоративные circle-иконки с «Для дома», «Для бизнеса», «Калькулятор», «Песни», «Разделы» (6 шт.), «Почему karaokeshop» (4 шт.). Разделы рефакторены в `.map()`
- **SearchOverlay** (`src/components/SearchOverlay.tsx`) — поиск в хедере: открывается по клику на лупу, фильтрует товары и статьи блога, быстрые ссылки при пустом запросе, закрывается по Esc
- **Дилерская полоса** — под бенто-сеткой главной: кликабельные бейджи [A] AST и [SE] Studio Evolution + «С 2012 · Алматы». Герой: бейдж «Официальный дилер AST и Studio Evolution». Футер: бейджи над копирайтом
- **Фавикон** (`src/app/icon.svg`) — SVG микрофон, фон `#0d1117`, цвет `#2dd4bf`
- **Lottie-анимация** — `lottie-react` установлен, `public/lottie-mic.json` (70KB), `LottieHero.tsx` в герое справа снизу, `opacity-60`, только `lg+`. Заменить: положить новый JSON в `public/lottie-mic.json`
- **Хлебные крошки** — добавлены на `/blog`

### Изображения товаров
Фото с сайтов производителей в `public/products/`: `ast-250.jpg`, `ast-350-2.png`, `evobox-main.webp`, `evobox-plus-main.webp`. Поле `image` прописано в 4 товарах (AST Mini временно использует ast-250.jpg — своего фото у производителя нет). Заменить: положить файл → обновить `image` в `site.ts`.

### Инфраструктура
- **CI cron** `.github/workflows/ci.yml`: `schedule: "0 8 * * *"` — ежедневный авто-деплой в 11:00 Алматы для обновления новостей
- **Страниц:** 47 → 77 (было в начале сессии: 68 после пакета 2)

### Ожидает от заказчика
- Реальные фото товаров (медиа-кит от AST и Studio Evolution)
- Lottie-анимация «люди поют» — скачать JSON с lottiefiles.com и заменить `public/lottie-mic.json`
- Подключить кастомный домен `karaokeshop.kz` в Cloudflare
- ID счётчика Яндекс.Метрики (заменить `XXXXXXXX` в `layout.tsx`)
- Google My Business + 2GIS — карточки заведения (SEO против конкурента karaoke-shop.kz)

## Предыдущая сессия (2026-06-23) — фото товаров + лента историй в блоге

Фото AST-250/AST-350 с art-system.ru, Evobox/Evobox+ с studio-evolution.com — скачаны в `apps/web/public/products/`. Поле `image` прописано в 4 продуктах (ast-250, ast-mini, evobox, evobox-plus). AST Mini временно использует ast-250.jpg (у производителя модель не представлена на сайте).

Новый тип `StoryPost` + массив `storyPosts` (8 историй) в `site.ts`. Страница `/blog` — добавлена секция «Истории установок»: masonry-сетка карточек в стиле соцсетей (аватар-инициалы, текст, теги, лайки, ссылка на систему). Хлебные крошки добавлены на `/blog`.

## Предыдущая сессия (2026-06-23) — SEO-блог пакет 3: +8 статей (итого 26 блог-постов)

Добавлены 8 статей в `blogPosts` (`site.ts`), ~5 000 симв. каждая. Сборка: 68 → 76 страниц.
- Карао для гостиницы и банкетного зала (зонирование, акустика зала, гарантии)
- Карао для корпоратива (аренда vs покупка, технический райдер, ошибки)
- Как подключить карао к телевизору (HDMI, ARC, Bluetooth, типичные проблемы)
- Гарантия и ремонт карао-оборудования в Казахстане (что покрывает, стабилизатор)
- Световое оборудование для карао (PAR LED, лазеры, DMX)
- Как обновляются песни в карао (подписка, USB, онлайн, пиратские базы)
- Карао в Астане и Шымкенте (доставка, монтаж вне Алматы, сроки)
- Как рассчитать бюджет на карао для заведения (5 статей расходов, типовые бюджеты)

## Предыдущая сессия (2026-06-23) — floating orbs + AnimationToggle

HeroWave: 28 div-баров → 3 CSS floating gradient orbs (only `transform`+`opacity`, `will-change: transform`). Управление через `.anim-paused` класс на `<html>` + CustomEvent `kk-anim-toggle`/`kk-anim-state`. Новый компонент `AnimationToggle` — фиксированная кнопка ⏸/▶ рядом со ScrollToTop (`bottom-[72px] left-4`), глобально в layout.

## Предыдущая сессия (2026-06-23) — SEO-блог: +8 статей по ~5 000 симв. (итого 18, 68 стр.)

Пакет 2: 8 статей с плотным содержанием (~5 000 символов каждая, без воды). Страниц: 60 → 68.
- Карао для бани и сауны (IP-защита, расстановка, кабели)
- Сколько стоит оснастить ресторан (детальные цены по площадям)
- Карао для клуба (проект звука, RCF, DSP, бюджет)
- AST-250 vs AST-350 (технические отличия, реальные сценарии)
- Беспроводные микрофоны: Shure BLX24/QLXD24 vs Rolenz (сравнение)
- Акустика: The Box, HK Audio Polar 10, RCF EVOX 8 (выбор под зал)
- Как выбрать подрядчика для монтажа (8 конкретных вопросов)
- Карао для гостевого дома и летней беседки (уличные условия)

## Предыдущая сессия (2026-06-23) — SEO-блог: +7 статей (итого 10)

Добавлены 7 новых статей в `blogPosts` (`site.ts`). Страниц: 53 → 60. Кластеры:
- **How-to/гайды:** «Как выбрать домашнее карао», «Как оснастить кафе под ключ», «Как настроить звук в зале», «Чек-лист приёмки»
- **Сравнения:** «AST Mini vs Evobox для дома», «Evobox Plus vs AST-250 для кафе»
- **Цены/гео:** «Сколько стоит домашнее карао в Казахстане»

## Предыдущая сессия — P3 завершён (#24–27)

P3#27 — `/kontakty` редизайн: карточка контактов + WhatsApp CTA + блок быстрых ссылок + карта; `ScrollToTop` (левый нижний угол, появляется после 400px скролла) добавлен глобально в layout; кастомная `not-found.tsx` (404) с иконкой Music и кнопками «На главную» / «Каталог».

P3#25 — `/keysy` переработан: Unsplash убран → `Building2` градиент-заглушка; `Badge` из `@kk/ui` → нативный span; FAQ → `FaqAccordion`; добавлен `breadcrumbJsonLd`.

P3#24 — `/gotovye-resheniya` почищен: `Button` из `@kk/ui` → нативные ссылки; добавлен `breadcrumbJsonLd`; импорт `gotovyeResheniyaMeta`.

P3#26 — тёмная тема уже была реализована ранее (ThemeToggle в Header + MobileNav + no-FOUC скрипт в layout).

## Предыдущая сессия — P2 завершён (#16–23)

P2#23 — `/product/[slug]` — добавлена секция «Похожие товары»: до 3 товаров того же бренда + до 6 итого альтернатив того же типа от других брендов. Рендерится через `ProductCard`.

P2#22 — `/pod-klyuch` переписан: ночной герой с УТП + цена-от + CTA; 3 шага как numbered-карточки (Проект/Монтаж/Обучение); что входит (8 пунктов); 4 числа доверия; `AreaCalculator` + ссылка на калькулятор; FAQ через `FaqAccordion` (добавлен пункт про поэтапную оплату); ночной CTA блок.

P2#21 — `/o-nas` переработан: убран Unsplash-герой (заменён текстовым блоком с primary-бордером), числа вынесены в крупные плашки (4 кол.), команда — инициалы вместо Unsplash-аватаров, CTA-кнопки без `@kk/ui Button`. Таймлайн сохранён.

P2#20 — `/brand/[slug]` переписан: герой с УТП, цена-от, highlights, warm/night тема по бренду; новый клиентский компонент `BrandProductFilter` (сценарные чипы Все/Дом/Кафе-бар/Клуб + `ProductCard` сетка).

P2#19 — `/sravnenie` переписан как редакционная страница: таблица сравнения (14 критериев, AST vs Studio Evolution, иконки ✓/✗/—/текст), блок «Сильные стороны» каждого бренда, блок «Что выбрать по сценарию» (дом / кафе / клуб), CTA WhatsApp + калькулятор.

P2#18 — `/dlya-doma` и `/dlya-biznesa` переписаны: герой с УТП + цена-от + WhatsApp CTA + иконки УТП; карточки товаров заменены на `ProductCard` (с WhatsApp-кнопкой); FAQ заменён на `FaqAccordion`. Убраны `Button` из `@kk/ui` в пользу прямых `<a>` и `<Link>`.

## Предыдущая сессия (2026-06-23) — P2#16–17: JSON-LD + SEO мета

P2#17 — улучшены title/description на 3 страницах:
- catalog: "Купить системы AST и Studio Evolution — каталог с ценами | Алматы" + rich description с числами
- dlya-doma: ценовой сигнал "от 749 000 ₸" + конкретные модели в title
- dlya-biznesa: "200+ проектов с 2012" + "Расчёт бесплатно" в description
Обновлено и в `site.ts` (dlyaDomaMetaV2, dlyaBiznesaMeta, catalogMeta), и инлайн в page.tsx.

## Предыдущая сессия (2026-06-23) — P2#16: JSON-LD обогащение

P2#16 — `seo.ts`: `productJsonLd` принимает `rating`/`reviewsCount`, добавляет `aggregateRating` + `seller`. `layout.tsx` LocalBusiness: добавлены `@id`, `geo` (43.2567, 76.9286), `addressRegion`, `sameAs` (2GIS placeholder). BreadcrumbList JSON-LD добавлен на: catalog, dlya-doma, dlya-biznesa, servis, o-nas, pod-klyuch. Страница товара передаёт `rating`/`reviewsCount` в productJsonLd.

## Предыдущая сессия (2026-06-23) — P1#8–15: + калькулятор 3 шага

P1#15 — `CalculatorClient.tsx` переписан как 3-шаговый wizard: Шаг 1 — сценарий (5 карточек), Шаг 2 — площадь (слайдер + быстрые кнопки), Шаг 3 — ориентир по бюджету (4 варианта). Смета на шаге 4: таблица позиций + итого + WhatsApp-кнопка со сметой в тексте. StepIndicator с чекбоксами пройденных шагов.

## Предыдущая сессия (2026-06-23) — P1#8–14: + servis гарантия + FAQ аккордеон

P1#14 — `servis/page.tsx`: блок «Что покрывает гарантия» (два столбца: покрывается ✓ / не покрывается ✗), FAQ заменён на `FaqAccordion.tsx` (клиентский, `useState` для открытого пункта, ChevronDown анимация), добавлен 5-й вопрос про выезды.

## Предыдущая сессия (2026-06-23) — P1#8–13: drawer + FAB + footer + товар + каталог + отзывы

P1#13 — `page.tsx` отзывы: убраны Unsplash-аватары, добавлен контекст установки (модель · тип объекта · город), звёзды рейтинга, ссылка «Все отзывы на Google» (desktop header + mobile footer).

## Предыдущая сессия (2026-06-23) — P1#8–12: drawer + FAB + footer + товар + каталог

P1#12 — `CatalogClient.tsx` переписан: поиск наверху (по модели и бренду, с кнопкой сброса), горизонтальные чипы-фильтры по типу (Все + 5 типов), shimmer-скелетоны через `useTransition` при фильтрации, убран sidebar с чекбоксами.

## Предыдущая сессия (2026-06-23) — P1#8–11: drawer + FAB + footer + страница товара

P1#11 — `product/[slug]/page.tsx`: desktop split (`lg:grid-cols-[1fr_420px]`), фото слева (h-520px), детали справа `lg:sticky lg:top-[72px]`, desktop CTA — WhatsApp + AddToCart + консультация. `ProductStickyBar.tsx` (новый) — fixed bottom bar на мобиле: цена + WhatsApp. `pb-28 lg:pb-10` чтобы контент не прятался за баром. `ProductImage` вместо Speaker-заглушки.

## Предыдущая сессия (2026-06-23) — P1#8–10: drawer + FAB + footer 3 колонки

P1#10 — `Footer.tsx` переписан: 3 колонки (О нас | Каталог | Контакты) на `sm:grid-cols-3`, копирайт внизу с `new Date().getFullYear()`.

## Предыдущая сессия (2026-06-23) — P1#8–9: drawer + FAB скролл + тема в drawer

P1#9 — `WhatsAppFAB.tsx`: появляется после 300px скролла (`opacity`+`translate-y` transition), виден и на десктопе. `MobileNav.tsx`: добавлен `ThemeToggle` в footer drawer рядом с телефоном.

## Предыдущая сессия (2026-06-23) — P1#8: мобильный drawer

`MobileNav.tsx` переписан: полноэкранный drawer (slide from left), `body overflow:hidden` при открытии, активный пункт `bg-primary/10 text-primary`, WhatsApp-кнопка + телефон/часы в футере drawer.

## Предыдущая сессия (2026-06-23) — редизайн P0 COMPLETE (задачи 1–7)

P0#7 — `apps/web/src/app/layout.tsx`: Яндекс.Метрика через `next/script` (strategy="afterInteractive"), вебвизор включён. Константа `YM_ID = "XXXXXXXX"` — заменить на реальный ID счётчика.

## Предыдущая сессия (2026-06-23) — редизайн P0#1–6: навигация + WhatsApp + Trust + Hero + Compare + Placeholder

P0#6 — новый компонент `ProductImage.tsx`: показывает `<img>` если есть `src`, иначе серый градиент с названием модели. Все Unsplash-URL удалены из `site.ts` (sed). `ProductCard` и editorial-блок главной используют `ProductImage`. Фото товаров добавляются через `public/products/<slug>.jpg` + поле `image` в `site.ts`.

## Предыдущая сессия (2026-06-23) — редизайн P0#1–5: навигация + WhatsApp + Trust + Hero + Compare удалён

P0#5 — удалён весь compare-слой: роут `/sravnit`, компоненты `CompareToggle`/`CompareBar`/`CompareClient`, либа `lib/compare.tsx`, `CompareProvider` из `Providers.tsx`, `<CompareBar>` из `layout.tsx`, `CompareToggle` из страницы товара, `/sravnit` из sitemap-списка `site.ts`.

## Предыдущая сессия (2026-06-23) — редизайн P0#1–4: навигация + WhatsApp + Trust + Hero цифры

P0#4 — `apps/web/src/app/page.tsx`: в hero-ячейку под CTA добавлен ряд из 3 цифр: «с 2012 года на рынке · 200+ проектов · 2 дня монтаж», цвет night-fg/night-muted.

## Предыдущая сессия (2026-06-23) — редизайн P0#1–3: навигация + WhatsApp CTA + Trust-блок

P0#3 — `apps/web/src/app/page.tsx`: добавлен Trust-блок (4 цифры) сразу после бенто-сетки: 12+ лет · 200+ установок · 60 000+ песен · 2 года гарантия. 2×2 на мобиле, 4 колонки на sm+.

## Предыдущая сессия (2026-06-23) — редизайн P0#1+2: навигация + WhatsApp CTA в карточке

P0#1 — навигация 7→5 + активный пункт (см. ниже).
P0#2 — `ProductCard.tsx`: убран `CompareToggle`; добавлена кнопка «Узнать цену» (WhatsApp) с предзаполненным сообщением «Здравствуйте! Интересует {model}...»; `"use client"` для `onClick` (stopPropagation); `wa.me/${siteConfig.whatsapp}?text=...`.

## Предыдущая сессия (2026-06-23) — редизайн P0#1: навигация 7→5 + активный пункт

Начат редизайн-спринт (39 задач, брейнсторм завершён). Выполнена P0-задача №1:
- `apps/web/src/lib/site.ts` `mainNav`: 7 пунктов → 5 (`/catalog`, `/dlya-doma`, `/dlya-biznesa`, `/pod-klyuch`, `/kontakty`); убраны `/brand/studio-evolution`, `/brand/ast`, `/gotovye-resheniya`.
- `apps/web/src/components/Header.tsx`: добавлен `"use client"` + `usePathname()`; активный пункт выделяется `border-b-2 border-primary`; неактивные — `text-muted-foreground hover:text-foreground`.
- Сборка и тесты зелёные.

## Предыдущая сессия (2026-06-23) — все mood-страницы следуют теме; Mood удалён

Распространён фикс pod-klyuch на остальные принудительно-mood страницы:
- Снята обёртка `<Mood variant=…>` с `komplekty`, `komplekty/[area]`, `karaoke/[scenario]` — теперь следуют теме сайта (светлая в светлой). Убраны неиспользуемые импорты и `const variant` в karaoke.
- Компонент `components/Mood.tsx` **удалён** (0 использований) — закрывает фут-ган повторного полностраничного форса. Класс-блоки `.mood-*` в `theme.css` оставлены безвредными; **переменные `--warm-*`/`--night-*` сохранены** (используются инлайн в бенто главной, dlya-biznesa, gotovye-resheniya, HeroWave).
- Проверено: build web, тесты web(10), визуал — komplekty light полностью светлая; бенто главной (warm/dark акцент-карточки) цело.

## Последняя сессия (2026-07-02) — главная: цена+кнопка карточек → акцент-токен; /dlya-biznesa: hero без «обводки»

- **Главная, секция «На чём собираем» (`page.tsx`):** в featured-карточках цена и кнопка «Подробнее» использовали mood-цвет (`--warm-accent` терракот для дома / `--night-accent` для бизнеса — хардкод, не по теме). Заменил на тематический акцент: цена `text-primary`, кнопка `bg-cta text-cta-fg` (графит+белый в светлой, жёлтый+тёмный в тёмной). Бейдж сценария на фото оставил в mood-цвете. Проверено обе темы.
- **/dlya-biznesa hero (`page.tsx`):** «обводки» как CSS не было — эффект рамки давал светлый верх градиента `#1e2b40` на тёмной странице `#0e131c`. Сменил градиент на `linear-gradient(180deg, #1b2438 0%, #0e131c 100%)` — низ = цвет страницы, карточка растворяется в фоне, рамка-эффект ушёл. В светлой теме остаётся тёмной (как задумано).

## Последняя сессия (2026-07-02) — /pod-klyuch: кнопка заявки → зелёный WhatsApp

В `AreaCalculator.tsx` («Быстрый расчёт по площади») кнопка «Оставить заявку в WhatsApp» была общим `<Button>` (жёлтый акцент). Заменил на зелёный WhatsApp-`<a>` (`bg-[#25D366] … text-white hover:bg-[#1ebe5d]`) — как остальные WhatsApp-CTA. Импорт `Button` из `@kk/ui` убран (был единственным использованием).

## Последняя сессия (2026-07-02) — /dlya-doma: «60 000+ песен» — счётчик

В hero-карточке `/dlya-doma` строка «60 000+ песен» была статичной — на главной такой же счётчик оживает. Обернул в `<CountUp value="60 000+ песен" />` (`dlya-doma/page.tsx`, +импорт). Анимация 0→60 000 на скролле, `prefers-reduced-motion` уважается, SSR отдаёт финальное значение (работает без JS).

## Последняя сессия (2026-07-02) — логотип: фиолетовая марка-иконка

По присланной иконке — логотип как скруглённый квадрат фиолетового цвета `#7c5cff` с белым микрофоном.
- `Header.tsx` — марка микрофона теперь в бейдже: `<span>` 28×28, `rounded-[8px]`, фон `#7c5cff` (инлайн, фиксированный бренд-цвет как WhatsApp-зелёный), внутри белый `Mic2`. Вордмарк `karaokeshop` без изменений.
- `app/icon.svg` (фавикон) — приведён к марке: фон `#7c5cff`, микрофон белый (было тёмное `#0d1117` + тил `#2dd4bf`). Теперь вкладка = логотип.
- Цвет фиксированный в обеих темах (не зависит от токенов акцента). Проверено в превью: `bg=rgb(124,92,255)`, `radius=8px`, `28×28`, белый mic.

## Последняя сессия (2026-06-23) — Kaspi/рассрочка удалена полностью (Задача 6)

Рассинхрон закрыт удалением (по решению владельца) — код совпал с CLAUDE.md «удалена полностью»:
- `Hero.tsx` — убрано «рассрочка Kaspi» (×2); `ProductCard.tsx` — убрана строка `от X/мес` (тернар → скидка показывает старую цену, иначе ничего) + импорт `installmentMonthly`; `CompareClient.tsx` — убрана строка «Рассрочка» + импорт; `komplekty/[area]` — убран бейдж рассрочки + импорт `CreditCard`; `site.ts` — функция `installmentMonthly` удалена, FAQ-пункт про рассрочку убран, ~7 SEO-описаний переписаны без Kaspi.
- Проверено: grep 0 вхождений, build web (импорты подчищены), тесты web(10), визуал каталога (карточки без `/мес`, верстка цела).

## Последняя сессия (2026-06-23) — фон страницы → токен --color-page

- Добавлен токен `--color-page` в `theme.css` (`#f5f5f5` light / `#0e131c` dark) + утилита `bg-page` в tailwind.config.
- Убран хардкод: `globals.css body` → `var(--color-page)` (удалён отдельный `.dark body`); Header/Footer `dark:bg-[#0e131c]` → `dark:bg-page`. Один источник фона страницы.
- Проверено: build web + визуал light (белый хедер на `#f5f5f5`) и dark (хедер сливается с `#0e131c`). Регрессий нет.

## Последняя сессия (2026-06-23) — /pod-klyuch следует теме (убран форс mood-night)

- **Баг:** `/pod-klyuch` был обёрнут в `<Mood variant="night">` → принудительно тёмная палитра НЕЗАВИСИМО от темы. В светлой теме = тёмный «остров» внутри светлого шелла (светлый body + светлый футер) → «страница не собралась», футер не совпадал.
- **Фикс:** убран `<Mood variant="night">` из `pod-klyuch/page.tsx` — страница следует теме сайта (светлая в светлой, тёмная в тёмной), как servis/catalog/kontakty. Проверено визуально в обеих темах: light — всё светлое и футер совпадает; dark — обычная тёмная палитра, карточки видны.
- ✅ **Тот же паттерн исправлен и на** `komplekty`, `komplekty/[area]`, `karaoke/[scenario]` — см. запись ниже (обёртка `Mood` снята со всех, компонент удалён).

## Последняя сессия (2026-06-23) — раздел C добит: icon-guard везде + чистка хардкод-dark-текста

- **C4 (распространён):** опциональная+guarded иконка во всех списках с `icon:` — `servis`, `dlya-doma`, `dlya-biznesa`, `o-nas`, `ServiceSteps`, `CalculatorClient`. Тип `icon?: LucideIcon` + `{Icon && …}`. Удаление иконки из данных больше не роняет карточку нигде.
- **C2 (добит):** убраны все хардкод `dark:text-[#…]` / `dark:text-white` / `dark:hover:text-white` в `Header.tsx`, `Footer.tsx`, `layout.tsx` (body). Текст темизируется через `text-foreground`/`text-muted-foreground` (флип в `theme.css`). Проверено визуально в dark — Header/Footer читаемы.
- Правила в CLAUDE.md: добавлены конвенции «иконки в списках — guard» и «темизируемый текст — только токен-утилиты». Чек-лист раздела C закрыт (C0–C4 ✓).
- Остаток (вне C): фон страницы `#0e131c`/`#f5f5f5` — хардкод в Header/Footer/globals (кандидат на `--color-page`); Kaspi-рассинхрон на LIVE (Задача 6, ждёт заказчика).
- Проверено: тесты ui(2)+web(10), build web + Storybook, визуал light/dark/mood.

## Последняя сессия (2026-06-23) — КОРНЕВОЙ ФИКС токенов + CI (раздел C fix-plan)

Диагноз «правлю в одном месте — в другом не применяется» / бесконечная правка тёмной темы → **двойной источник правды для цветов** (tokens.css дублировал globals.css, globals молча перебивал). Плюс «убрал иконку → пропала карточка» = связность import↔данные↔рендер. Фиксы:
- **C1 (корень):** цвета вынесены в единый `packages/tokens/css/theme.css` (light/`.dark`/`.mood-*`), импортится приложением (`layout.tsx`) И Storybook (`preview.ts`). Цвета убраны из `tokens.json`/`tokens.css` (там теперь только радиусы) и из `globals.css` (только база + тени). Правка цвета теперь в ОДНОМ месте. Проверено: build web + Storybook + визуал light/dark/mood.
- **C2:** `.dark body` вернулся на `var(--color-fg)` (костыль-хардкод не нужен — конфликта нет).
- **C3:** clay-тень привязана ко ВСЕМ `.bg-background`, а не к `rounded-2xl/3xl` → смена радиуса больше не «теряет» карточку.
- **C4:** `servis/page.tsx` — иконка опциональна (`icon?: LucideIcon`) + guard `{Icon && …}`; удаление иконки из данных больше не роняет карточку.
- **C0 (CI):** `lighthouserc.json` performance `error`→`warn`. Деплой (job `build-test`) и так шёл; красный Lighthouse создавал ложное «не задеплоилось». Деплой от Lighthouse не зависит.
- Правило в CLAUDE.md обновлено: один источник на вид токена; цвета — только в `theme.css`.

## Последняя сессия (2026-06-23) — /servis: контраст текста в карточках

- `servis/page.tsx`: заголовок карточки `font-medium` → `text-base font-semibold`; тело `text-muted-foreground` → `text-foreground/70`

## Последняя сессия (2026-06-23) — dark: явный цвет текста на body

Корневая причина: tokens.css ставит `--color-fg: #0E1726` в `:root`; `.dark { --color-fg }` не всегда перебивает при статическом экспорте.

- `globals.css`: `.dark body { color: #f0f4fc }` — явно без var(), перебивает tokens.css
- `layout.tsx`: `<body dark:bg-[#0e131c] dark:text-[#f0f4fc]>` — Tailwind-фолбэк для статики

## Последняя сессия (2026-06-23) — Header/Footer: явный тёмный текст

- `Header.tsx`: nav и кнопка поиска → `dark:text-[#b8cfe0] dark:hover:text-white`
- `Footer.tsx`: основной текст → `dark:text-[#b8cfe0]`; заголовок и телефон → `dark:text-white`; ссылки → `dark:hover:text-white`

## Последняя сессия (2026-06-23) — dark theme: night-карточки + контраст текста

- `.dark` переопределяет `--night-bg: #1e2e48` (было фикс. `#0b1220` = темнее страницы → карточки тонули)
- `--color-bg: #233050` (ярче, чётче отличается от `#0e131c` страницы)
- `--color-fg: #f0f4fc` (14.2:1 ✓ AAA), `--color-muted-fg: #aac0d6` (8.2:1 ✓ AAA)
- `--color-primary: #4de3c8` (11.2:1 ✓ AAA) — ярче для `group-hover:text-primary`
- Тень расширена: `.dark .bg-background` (было только `.rounded-2xl/.rounded-3xl`)
- Night-бенто (inline style) получают `outline: 1px solid rgba(255,255,255,.12)` в тёмной теме

## Последняя сессия (2026-06-23) — CLAUDE.md: правило КОММИТ + ПУШ + ДОКИ

- `CLAUDE.md`: добавлен обязательный раздел «Правило: КОММИТ + ПУШ + ДОКИ — всегда в одном коммите» с таблицей что обновлять и чеклист расширен соответствующим пунктом

## Последняя сессия (2026-06-23) — Header/Footer: явная тёмная тема

- `tailwind.config.ts`: добавлен `darkMode: "class"` (ранее отсутствовал → `dark:` утилиты работали через media query, а не `.dark` класс)
- `Header.tsx`: `dark:bg-[#0e131c] dark:border-white/[0.08]` — хэдер сливается с фоном страницы в тёмной теме
- `Footer.tsx`: `dark:bg-[#0e131c] dark:border-white/[0.06]` — аналогично, убрана яркая `bg-surface` в тёмной теме

## Последняя сессия (2026-06-23) — globals.css: тёмная тема — читаемость карточек

- `--color-bg` (тёмная карточка): `#141c27` → `#1c2840` — заметнее отличается от фона страницы
- `--color-surface` (чипы): `#1e2a3a` → `#263550`
- `--color-fg`: `#e6eaf1` → `#edf2fa` (**12.5:1** ✓ AAA)
- `--color-muted-fg`: `#94a3b8` → `#a0b8cc` (**7.6:1** ✓ AAA — было тускло на тёмном фоне)
- `.dark body` фон: `#0b0f14` → `#0e131c`
- Ring-тень: `rgba(255,255,255,.10)` → `.12`, shadow `8px` → `12px`

## Последняя сессия (2026-06-23) — globals.css: светлая/тёмная тема, WCAG-контраст

> Также: trust-блок на главной («С 2012 / Гарантия / Монтаж»)  теперь `<Link href="/servis">` — клик ведёт на `/servis`, hover показывает «Подробнее →».

### globals.css — единый источник правды
- **Светлая тема** задана в `:root` явно (переопределяет tokens.css). Ключевые значения:
  - `--color-bg: #ffffff` (карточки), тело страницы `#f5f5f5` hardcoded
  - `--color-muted-fg: #586070` — было `#64748B` = **4.6:1, провал AA** на `#f5f5f5`; стало **5.4:1 ✓ AA**
  - `--color-primary: #1f6f66` — **5.2:1 ✓ AA** на фоне карточки
- **Тёмная тема** (`.dark`): карточки `--color-bg: #141c27` теперь отличаются от фона страницы `#0b0f14`; кольцо-тень усилена до `rgba(255,255,255,.10)` + глубокая тень
- **`.mood-warm`**: `--color-muted-fg: #764e2a` — было `#8a6a4c` = **4.1:1 провал**; стало **5.7:1 ✓ AA** на warm-muted bg
- **Все `.mood-*`**: контраст задокументирован комментариями в файле

## Последняя сессия (2026-06-23) — Homepage: контраст карточек + единая система переменных

- **Проблема:** `bg-surface` (#F8FAFC) на `body` (#f5f5f5) = 1.03:1 — карточки сливались с фоном.
- Trust block («С 2012», «Гарантия + сервис-центр», «Монтаж и настройка»): `bg-surface` → `bg-background` + `font-medium`. Получают clay-тень из globals.css автоматически.
- «Как мы работаем» шаги: `bg-surface` → `bg-background`; нумерация `text-primary/20` → `/40` (читаема).
- `ServiceSteps.tsx`: `bg-surface` → `bg-background`.
- «Для дома» бенто-карточка: добавлен `border: 1px solid var(--warm-soft)` — визуально отделяется от серого фона.
- **Принцип:** нейтральные карточки = `bg-background` (белый + clay-тень); тёплые/ночные карточки = явные `var(--warm-*)` / `var(--night-*)`. Две системы переменных остаются, но каждая применяется строго к своей области.

## Последняя сессия (2026-06-23) — Аудит P0–P2: SEO, .gitignore, dead code, гигиена

- SEO: canonical на `/`, 301 `/karaoke/dlya-doma` → `/dlya-doma`, OG/Twitter meta в root layout, Product JSON-LD с image, BreadcrumbList на страницах товара.
- Lighthouse CI: 3 → 8 URL.
- `.gitignore`: `*.zip`, `*.tsbuildinfo`, `.claude/`, `скриныыы*/` добавлены; дубль `.next/` убран.
- Orphan `tokens/` (корень) удалён; build читает `packages/tokens/`.
- Dead components удалены: FilterRail, SectionHeading, TrustStrip.
- `.prettierrc` создан; `engines: { node: ">=20" }` и `format` скрипт в `package.json`; `turbo.json` обновлён.

## Последняя сессия (2026-06-23) — CI/CD: автодеплой на Cloudflare через GitHub Actions

- **Проблема найдена:** `out/` в `.gitignore` → `git push` никогда не деплоил файлы. Деплой требовал ручного `npx wrangler deploy` после каждой сборки.
- **Решение:** добавлен шаг деплоя в `.github/workflows/ci.yml` — `npx wrangler deploy` запускается только на `push` в `main`, после успешной сборки и тестов. Использует секрет `CLOUDFLARE_API_TOKEN`.
- **Секрет добавлен:** `CLOUDFLARE_API_TOKEN` в Settings → Secrets репо `airginggger-collab/Karaokeshop`. Токен с шаблоном «Edit Cloudflare Workers».
- **Новый флоу:** `git push origin main` → CI (тесты + сборка) → `wrangler deploy` → прод автоматически. PR-ветки деплой не триггерят.
- CLAUDE.md обновлён — старая инструкция «запускать wrangler вручную» заменена на описание CI-деплоя.

## Последняя сессия (2026-06-23) — Калькулятор: белый дизайн + экспертные хинты

- `CalculatorClient.tsx`: левая панель обёрнута в белую карточку `bg-background rounded-2xl p-6`. Убрана строка «рассрочка Kaspi».
- Чипы типа заведения переключены на `rounded-full` (активный — залитый primary).
- **Trust strip**: 4 плитки над калькулятором — мощность по площади, Shure UHF, XLR-кабели, 200+ объектов.
- **Хинты в смете**: каждая строка теперь содержит `hint` — экспертное объяснение почему этот компонент (добавлено в `Line` тип и `configure()` в `calculator.ts`). Рендерятся под названием позиции мелким серым текстом.

## Последняя сессия (2026-06-22) — CRO: квиз, логотипы, FAQ, микрокопии

### Новые компоненты
- `QuizWidget.tsx` — клиентский компонент, 3 вопроса (место/площадь/бюджет), прогресс-бар, результат открывает WA с ответами. Размещён на главной после ценовых тьеров.
- `ClientLogos.tsx` — секция «Нам доверяют» с 8 плейсхолдер-чипами заведений Алматы/Астаны. Размещена на главной перед «Разделами». **Заменить на реальные логотипы когда получим от клиента.**

### FAQ добавлен
- `/servis` — 4 вопроса + FAQPage JSON-LD
- `/pod-klyuch` — 4 вопроса + FAQPage JSON-LD + WA CTA с микрокопией

### Микрокопии под CTA
«Ответим в течение часа · Без обязательств» добавлена под WhatsApp-кнопками на:
dlya-doma, dlya-biznesa, gotovye-resheniya, servis, pod-klyuch.

## Последняя сессия (2026-06-22) — WCAG AA: warm-accent

- `globals.css`: `--warm-accent` #f97316 → **#c2410c** (orange-700).
  Было 2.65:1 на `--warm-bg` (#fff7ed) — провал. Стало **4.82:1** — проходит WCAG AA.
- Та же правка в `.mood-warm` (`--color-primary`/`--color-accent`).
- `gotovye-resheniya/page.tsx`: заменён inline-breadcrumb на `<Breadcrumb>`.
- Белый текст на `#c2410c` (кнопки) → 5.1:1 ✓ — ничего не сломалось.

## Последняя сессия (2026-06-22) — Checkout + Breadcrumbs

### Checkout (`/checkout`)
- Убрана вся рассрочка Kaspi из UI (payment radio, monthly text).
- `CheckoutClient.tsx`: новый флоу — форма имя/телефон → кнопка «Отправить в WhatsApp» открывает WA с составом заказа.
- Хлебная крошка добавлена, h1 обновлён на `font-display`.

### Breadcrumbs — новый компонент
- `src/components/Breadcrumb.tsx` — `items: {label, href?}[]`, всегда начинается с «Главная».
- Добавлен на **все** страницы: o-nas, keysy, dlya-doma, dlya-biznesa, pod-klyuch, kontakty, servis, pesni, kalkulyator, catalog, sravnit, sravnenie, brand/[slug], product/[slug], checkout.
- Для brand/[slug] и product/[slug] — двухуровневые крошки (Каталог → Продукт).

### Рассрочка удалена везде
- `product/[slug]`: убрана строка «Kaspi рассрочка от … × 12 мес», исправлен title/description metadata.
- `page.tsx`: убран неиспользуемый импорт `installmentMonthly`.

## Последняя сессия (2026-06-22) — Editorial product cards (Alphane Labs style)

- `page.tsx`: секция «Популярное оборудование» переделана под editorial 2×2 композицию.
  - Карточка: белый `bg-background`, фото-зона вверху `h-56/h-64` с `object-cover`, внизу "полка" с характеристиками.
  - Сценарий-бейдж (`scenarioLabel`) поверх фото — тёплый/ночной акцент.
  - Чипы характеристик (`до X м²`, `N+ песен`) в `bg-surface` пилюлях.
  - Цена акцентным цветом + CTA-кнопка с `gap` анимацией при hover.
  - Фильтр расширен: все `type === "sistema"` (4 продукта, не только featured), grid 2×2.

## Последняя сессия (2026-06-22) — Clay morphism / фон страницы

- `apps/web/src/app/globals.css`: `body` bg light → `#f5f5f5` (тёмная без изменений).
- Глобальные CSS-правила `.bg-background.rounded-2xl/3xl` → `box-shadow: 0 1px 3px rgba(0,0,0,.06)` — clay morphism вместо жёсткого border. Компоненты не трогались.
- `.dark` вариант: тень темнее + белый ring `rgba(255,255,255,.05)`.

## Последняя сессия (2026-06-22) — E-E-A-T / AI Overviews

### SEO / Schema
- **LocalBusiness JSON-LD** в `layout.tsx` (глобально на всех страницах).
- **FAQPage JSON-LD** на `/dlya-doma`, `/dlya-biznesa`, `/keysy`.
- Хелпер `faqJsonLd()` уже существовал в `src/lib/seo.ts`.

### Новый контент (E-E-A-T)
- **Главная (`page.tsx`):** добавлена секция «Как мы работаем» (5 шагов) + блок отзывов (3 карточки с Unsplash-портретами).
- **`/dlya-doma`:** FAQ-секция (4 вопроса) + FAQPage schema.
- **`/dlya-biznesa`:** FAQ-секция (4 вопроса) + FAQPage schema.
- **`/keysy`:** карточки кейсов с фото `h-48`, блок метрик (срок монтажа + результат), FAQ-секция внизу.
- **`/o-nas`:** команда (3 участника с Unsplash-портретами), таймлайн 2012–2026, стата «200+ объектов».

## Последняя сессия (2026-06-22) — feat/v2-homepage
- **v2-хоумпейдж:** новый H1 «Караоке без ошибки в выборе», обновлены hero CTA (Выбрать домой / Для заведения).
- **Навигация v2:** Для дома / Для бизнеса / Studio Evolution / AST / Готовые решения / Монтаж / Контакты.
- **Новые страницы:** `/dlya-doma`, `/dlya-biznesa`, `/gotovye-resheniya` (собственные файлы в `app/`).
- **Ценовые тьеры** на главной: от 749k (дом) / 1.4M (кафе) / 2.5M (клуб).
- **Блок разделов** обновлён под новую навигацию.
- **HeroWave** (`src/components/HeroWave.tsx`): ambient CSS-эквалайзер в hero, зацикленная анимация 28 баров, toggle «Анимация вкл/выкл», уважает `prefers-reduced-motion`.
- **Большие карточки оборудования** на главной: секция «Популярное оборудование», 3 карточки (AST-250 / AST Mini / Evobox) с фото на весь фон, hover-zoom, warm/night градиенты. Источник: `products.filter(p => p.featured && p.type === "sistema")`.
- **Бенто-сетка выровнена:** Для бизнеса col-span-2 (под Для дома), Калькулятор col-span-2, убрана карточка «Хит» (дублировала блок с большими фото). Нижняя строка: Калькулятор / Песни / Доверие — 3 × col-span-2.
- Ветка `main` — стабильная v1 (откат через `git checkout main`).

## Последняя сессия (2026-06-20)
- **Уборка репо:** случайно закоммиченный в корень скриншот снят с трекинга. `.gitignore` дополнен блокировкой stray-бинарников. Коммит `b59d283`.
- Документация (README/CLAUDE/docs) приведена к текущей реальности.

## Что готово (live)
Бенто-главная (2 настроения: тёплое «для дома» / деловое ночное «для заведений») · e-commerce с корзиной · B2B-воронка «под ключ» · **онлайн-калькулятор сметы** (2 режима, реальные модели поставщика) · каталог 18 товаров (системы + акустика/микрофоны/сабвуфер/микшер) с фильтрами по типу/бренду · **сравнение оборудования** (`/sravnit`) и **сравнение брендов** (`/sravnenie`) · блог с FAQ-разметкой · кейсы · о компании · контакты с картой · песни · тёмная тема · мобильное меню · SEO (JSON-LD, sitemap, hreflang-структура).

## Что дальше / открытые хвосты
1. **Цены — оценочные.** Поставщик (show-service) **скрывает цены** в каталоге. Числа в `components.ts`/товарах привязаны к подтверждённым «готовым решениям» (551 800 / 611 800 ₸). Заменить по счёту поставщика.
2. **Фото товаров — демо (Unsplash).** Заменить на реальные: `public/products/<slug>.jpg` + поле `image` в `site.ts`.
3. **Логотипы клиентов** — `ClientLogos.tsx` содержит 8 текстовых плейсхолдеров (Шафран, Nomad, Babylon…). Заменить на реальные SVG/PNG когда клиент пришлёт.
4. **CMS** (Sanity vs Strapi) — ждёт ответ заказчика про резидентность данных (ADR-0001). Сейчас контент захардкожен в `site.ts`.
5. **Бриф заказчика** — частоты Wordstat (семантическое ядро), аналитика, доступы. См. [docs/client/client-brief.md](client/client-brief.md).
6. **Кастомный домен** `new.karaokeshop.kz` — ждёт доступ владельца к DNS (потом 2 клика в Cloudflare).
7. **Бейджи сценария в featured-карточках главной** — всё ещё в mood-цвете (терракот/жёлтый), а не в акцент-токене (цена+кнопка уже переведены 2026-07-02). Привести к акценту — по желанию.
8. **`YM_ID` = placeholder** (`XXXXXXXX`) — Яндекс.Метрика под гардом `YM_ENABLED`, реально не грузится. Подставить реальный ID от владельца (см. `client/chto-nuzhno-ot-vas.md`).
9. **`siteConfig.url` ведёт на непривязанный `www.karaokeshop.kz`** — canonical/og указывают на старый Wix. Закроется при привязке домена; до тех пор не трогать.

_(Закрыто: `/sravnenie` уже в `allPaths()` → в sitemap — прежний хвост про «/sravnit» неактуален.)_

## Подводные камни
- ⚠️ Это **не medlog** — коммить через `git -C ~/Desktop/karaokeshop` (был случай ошибочного коммита в medlog).
- **show-service — поставщик, на сайте НЕ упоминается** (использован как справочник номенклатуры).
- Цены — оценочные; демо-фото — временные.
- **`_local-assets/` — gitignored локальная свалка**, не ре-трекать. Корневые `*.png/*.jpg/*.jpeg/*.pdf` тоже игнорятся — не клади бинарники в корень.
- **`deploy.md` исторически описывал Cloudflare Pages (Connect-to-Git)**, но реальный продакшен — assets-only **Worker** (`wrangler.toml`, URL `*.workers.dev`). Источник истины по деплою — `wrangler.toml`.

## Документация
[docs/README.md](README.md) (индекс) · [/CLAUDE.md](../CLAUDE.md) (правила для AI) · [strategy/](strategy/) (стратегия, ядро, бюджет, URL-карта) · [plans/](plans/) · [deploy.md](deploy.md) · [adr/](adr/) · [redaktirovanie-sajta.md](redaktirovanie-sajta.md) (правка владельцем).
