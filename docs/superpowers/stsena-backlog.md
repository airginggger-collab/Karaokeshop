# Дизайн-спринт «Сцена» — бэклог (should/nice, не блокеры)

> Спринт Task 1–6 закрыт чисто 2026-07-06 (HEAD `597319a`). Ниже — уточнения дизайн-суждения из финального адверсариального ревью, которые НЕ вошли в спринт (требуют вкуса/решения владельца, не механические). Приоритет — на усмотрение.

## Should — глубже уйти от «шаблонности» (второй проход анти-AI)

1. **Симметричные стат-гриды «число+подпись».** `o-nas/page.tsx` (~93–100), `pesni` (~24–40) — ряд одинаковых карточек в ровной grid = узнаваемый SaaS-шаблон. Идея: сломать симметрию (тикер-полоса, разный размер, hairline-строка вместо карточек).
2. **Повтор «иконка-в-кружке + заголовок + абзац».** Сквозной юнит `rounded-full bg-primary-soft` + иконка на 5+ страницах (`servis` ~79–88, `dlya-doma`, `dlya-biznesa`, `pod-klyuch`, `o-nas`, `blog`). Идея: дифференцировать 2–3 (нумерация `01/02`, hairline-иконка без заливки, HighlightLine на заголовке).
3. **Три идентичных hero «текст + скруглённая сцена-плашка».** `page.tsx` (~118), `dlya-doma` (~76), `dlya-biznesa` (~98) — одинаковая композиция с бледной надписью `text-primary/40`. Идея: ввести асимметрию / разное наполнение плашек, чтобы не читались как один шаблон × 3.

## Nice — мелочи

4. **`(pages)/[page]/page.tsx` (~36)** — highlight = первое слово h1 (эвристика); может лечь на предлог. Задать highlight явно на каждую static-страницу (как для лендингов).
5. **`not-found.tsx` (404)** — токен-чистая, но без HighlightLine на «404»/заголовке. Опционально.
6. **`CountUp.tsx`** — компонент-одометр без потребителей. СОХРАНЁН намеренно (спек-правило №2: «компонент и тесты остаются в репо»). Не удалять; при желании — заметка «не подключать к стат-гридам».

## Осознанно НЕ трогаем (проверено, не баг)

- Чипы характеристик товара («до X м²», «N+ песен») на карточках и хэштеги статей (`#tag`) — функциональные семантические чипы, НЕ запрещённые trust-капсулы. Запрещённые ряды из hero убраны в Task 3.
- `#17233f`/`#facc15` (+ бордер `#2a3852`) в `Header.tsx` и `icon.svg` — марка лого (разрешено; перекрашена 2026-07-07 из фиолетовой `#7c5cff`+белый под новую палитру).
- `#25D366`/`#1ebe5d` — только за WhatsApp-кнопками (проверено поштучно, индикаторы наличия переведены на `text-success` в Task 6d).

## Отдельные треки (не дизайн — из аудита 2026-07-04)

- Лид-пайплайн: заявки живут только в WhatsApp без сохранения. Нужен endpoint (Cloudflare Worker) — пересекается с задачей «личный кабинет».
- Аналитика: Метрика-заглушка `XXXXXXXX`, GA4/GSC нет.
- Юридика: политика/оферта/реквизиты отсутствуют (обязательны для магазина РК).
- Реальные фото/отзывы/кейсы (ждём заказчика; секции готовы под флагами `SHOW_UNVERIFIED_*`).
- Домен `.kz` не привязан (отдаёт старый Wix).
